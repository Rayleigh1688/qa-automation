# 会员详情：钱包账变

[详情入口](README.md) · [逐页计划](plan.md)

2026-09-17 11:45—11:46（UTC+8）FAT页面发现及fresh API只读核验。用户要求跳过功能限制，按页签顺序进入钱包账变。无资金操作、流水清零或新增投注。

## 页面范围

- 搜索维度：流水ID/来源单号，下拉切换，输入提示0/5000；账变类型；账变时间；账变金额/期初金额/期末金额范围；备注0/5000；查询/重置。
- 时间默认当日00:00:00—23:59:59；本轮页面日期2026-09-17。5000为UI提示，不代表后端长度已测。
- 列：序号、流水ID、会员账号（按钮）、UID、会员手机、账变类型、来源单号、账变时间、账变金额、期初金额、期末金额、备注。
- 分页默认20条；有刷新、行密度、列设置入口，未展开；当前没有看到导出按钮，不推断接口无导出能力。
- 有“流水要求清零”按钮：资金/流水写操作单列待测，本轮未点击，不因它出现在查询页而自动执行。
- 当日页面1条发放礼金：金额+3，期初863911.76、期末863914.76；页面备注说明投注返利与3元流水要求。仅页面证据，不据此宣称返利计算正确或完成来源订单对账。

## 接口映射与只读结果

来源：api/catalog/admin/finance.csv及本机接口库“后台/财务管理/会员钱包账变”docs段。

POST /admin/finance/transaction/list；GET /admin/finance/transaction/types。文档写所有筛选走URL Query，现有目录为POST，本轮用CBOR body实测筛选有效；尚未捕获页面真实请求，不把实测可用方式等同唯一契约。

参数：uid、id、bill_no、cash_type、start_time/end_time（毫秒）、page/page_size；amount_min/max、before_amount_min/max、after_amount_min/max、remark（文档定义模糊查询）。列表UID/手机号搜索优先级规则不自动套用本账变页。

本轮指定配置会员，时间2026-09-16 00:00:00—2026-09-17 23:59:59（UTC+8），page=1/page_size=20。证据摘要位于本地忽略目录`reports/qa/member-detail/20260917/wallet-readonly.json`，无原始个人资料/token。

| 核验 | 结果 | 边界 |
| --- | --- | --- |
| 基线 | HTTP200/status=true，data为d/s/t，2条，总数2 | 仅该会员该窗口 |
| UID/时间 | 两条均属于目标UID，时间均在范围内 | 不是跨角色越权验证 |
| 余额公式 | 两条均满足before_amount + amount = after_amount | Decimal精确比较；一正一负，不证明所有类型或余额链完整 |
| 唯一性 | 返回流水ID不重复 | 仅当前两条 |
| id筛选 | 1条非空且id/UID匹配 | 未测批量/非法值 |
| bill_no筛选 | 1条非空且单号/UID匹配 | 未对来源业务订单独立查账 |
| cash_type筛选 | 1条非空且类型/UID匹配 | 未覆盖全部类型 |

## 用例与后续缺口

- MD-W01基础列表/UID/时间/金额公式及三个精确筛选：本样本已核。
- MD-W02金额上下界（含负数/0/精度）、期初/期末范围、备注模糊、时间边界：文档已有可设计依据，继续补查，无需先向用户重问常规查询逻辑。
- MD-W03非空多页、排序、重置、类型枚举及空集：待补数据/实际请求。
- MD-W04来源单号→充值/提现/投注/礼金/人工账变原订单，金额方向、重复记账/撤销、余额连续性：待按业务类型逐项核验，不从内部公式正确推断对账完成。
- MD-W05流水清零的适用记录/会员范围、是否影响钱包、所需审计：先查既有受控流水资产与契约再提问；本轮不操作。
- 无认证/低权限、批量查询、全部账变类型、来源订单对账、清零未执行。当前未发现新的已确认缺陷，不标记整页通过。

## 2026-09-17 13:12 边界核验发现异常，停止推进

用户要求无问题继续、有问题及时停下。本轮fresh FAT只读执行，基线仍为同会员同两日窗口2条。脱敏证据：`reports/qa/member-detail/20260917/wallet-boundaries.json`。

1. 取基线第一条记录的amount，将amount_min/amount_max同时设为其精确值，返回1条，ID与预期一致。
2. 取同条记录的before_amount，将before_amount_min/before_amount_max同时设为其精确值（Decimal转字符串，不经过浮点），预期基线中同值记录1条，实际HTTP200/status=true但返回0条。
3. 命中集合不符后立即停止；未执行期末金额、负数/零金额、时间边界或分页测试，未进入注单明细。

请求POST /admin/finance/transaction/list，CBOR body；保留uid、start_time/end_time、page=1、page_size=20，分别添加金额范围。异常请求仅加before_amount_min/max，不夹带前一条amount_min/max。参数名来自现有Bruno文档。

分类：期初金额精确范围筛选异常候选。尚未捕获页面同条件请求或核Query/body及边界语义，不据此直接认定页面BUG/后端根因。证据摘要保存命中数量与集合断言，未保存原始UID/交易明细及精确请求金额；如需人工复现，先从同时间范围基线任选可见记录，复制其期初金额到范围两端。

下一步停在此项：对照页面“期初金额”同值范围查询及真实参数，区分页面/契约/后端问题；不继续扩大测试或写业务数据。此前两条金额公式与三个精确字段筛选结果保持，不因新异常撤销，也不把整页标为通过。

## 用户确认与继续（2026-09-17）

用户已确认期初、期末金额筛选确实有问题，并已安排提BUG；编号未提供，不记已建单或已修复。期初有本轮API证据，期末来源为用户确认，不能冒充本轮期末API实测。按用户授权继续下一页，既有缺陷与未测项保留。
