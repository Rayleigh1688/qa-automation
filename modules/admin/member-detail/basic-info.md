# 会员详情：基础信息

[推进计划](plan.md) · [详情入口](README.md)

2026-09-17 11:00—11:01（UTC+8），复用FAT后台当前会话，打开既有会员详情；仅展开编辑/历史后取消关闭，没有输入或保存资料。是入口发现，不是写流程PASS。

| 对象 | 当前可见行为 | 后续API验证 |
| --- | --- | --- |
| UID/账号 | 默认详情展示；UID可复制 | 同列表UID定位，不串会员，身份字段是否允许改以接口规则为准 |
| 昵称/性别 | 用户信息编辑弹窗只有昵称、性别，两项标必填；昵称显示0/60；Cancel/OK | 确认字符/字节限制、空值/枚举、权限、生效与审计；未测保存 |
| 邮箱 | 点击编辑变成输入框及勾选/关闭；本轮点关闭退出 | 格式/唯一性、清空、验证状态、生效与审计，确认接口后执行 |
| 手机号历史 | 弹窗列为序号、手机号、更新时间；当前样本No data | 精确UID及变更类型，空集不漏他人数据；缺历史样本不证明历史功能通过 |
| 真实姓名历史 | 已发现“查看历史”入口，未打开 | 与KYC正式资料/复核状态关联，不能把待复核草稿当正式姓名 |
| 关联账号/账号类型/会员状态 | 展示字段 | 各自数据源与枚举不混用；限制规则引用会员列表 |

候选主接口GET /admin/member/detail、历史GET /admin/member/change/logs、更新POST /admin/member/update，来自现有会员接口目录；本轮未捕获实际按钮请求，不将候选路径当作已验证映射。

规则核对队列（先查文档，再集中提问）：昵称/性别及邮箱修改是立即生效还是另有复核；邮箱校验/验证状态如何变化；手机/姓名历史分别覆盖哪些修改来源。0/60为页面提示，真实最大长度及计算单位需实证，不自动定为60字节。KYC编辑规则不能直接套用到普通基础资料编辑。

## 2026-09-17 基础契约与只读核验

11:18—11:19（UTC+8）FAT fresh API登录，按配置手机号唯一定位会员，再用返回UID查详情/历史。仅登录与只读查询，无会员资料修改。原始脱敏摘要：`reports/qa/member-detail/20260917/basic-readonly.json`（本地忽略目录）。初次历史解析误用了d/t，已保留为basic-readonly-initial-schema.json，不沿用其空集判断；最终按实际list/total重新核验。最初沙箱网络失败未产生业务结果，获准联网后执行。

| 检查 | 结果与边界 |
| --- | --- |
| 列表→详情身份 | UID、username、phone均存在且一致，手机号只命中一个会员 |
| 基础字段映射 | 列表nick_name→详情nickname；gender、email同名。三项一致但本样本均空，非空显示/长度/更新尚未验证 |
| KYC状态 | detail没有同名kyc_status，不能把detail.status直接当KYC状态；来源映射待补 |
| 手机/姓名历史 | GET /admin/member/change/logs，uid + change_type=0/1，均HTTP200/status=true，data含list=null、total=0；UI两种历史均No data。只是当前样本空集一致 |
| 详情无认证 | HTTP200/status=false，data为提示字符串，业务拒绝 |
| 历史无认证 | 同UID、change_type=0，无token仍HTTP200/status=true、total=0。鉴权疑点待定位；空样本不能证明历史数据泄露或访问隔离通过 |
| 真实姓名历史入口 | 弹窗包含序号、真实姓名、更新时间，已打开关闭；姓名变更来源及非空顺序未验证 |

本轮未测：编辑保存/回读/审计、非空历史及UID隔离、低权限账号、跨会员访问限制、邮箱验证、必填/长度/枚举非法值、并发修改。未改动已确认GGR问题，用户自行提Jira。

## 文档契约（候选，不等于按钮请求已捕获）

来源：本机接口库“后台/会员列表/更新会员 - seven-double.bru”和“获取会员变更记录.bru”的docs段，以及当前接口目录；未读取/复制其中凭据。

- POST /admin/member/update：uid必填，文档列nickname、gender、email及其他字段；gender文档枚举male/female/un_know。普通资料编辑仅据UI限定昵称/性别/邮箱，不把宽泛文档字段视为允许修改密码/手机号/状态。
- GET /admin/member/change/logs：change_type 0手机号、1真实姓名、2会员状态、3绑定方式。文档正文省略/admin且响应示例过简；本轮实测确认上述/admin路径及list/total结构，仅执行0/1。
- UI昵称0/60、昵称与性别必填为页面观察，尚未证明后端边界。普通资料编辑不自动继承KYC复核规则。

## 下一轮用例与前置

| 编号 | 场景 | 前置/预期及当前状态 |
| --- | --- | --- |
| MD-B01 | 唯一会员列表/详情身份一致 | 本样本已核对；非空昵称/性别/邮箱样本待补 |
| MD-B02 | 手机/姓名历史空集 | 本样本UI/API一致；正向非空、时间顺序及变更类型隔离未测 |
| MD-B03 | 无认证/低权限历史查询 | 无认证成功空集待定位；需已知有历史且获授权的测试会员，预期不返回受保护记录；低权限未测 |
| MD-B04 | 修改昵称/性别 | 专用可修改测试会员、长度/枚举/即时生效或复核规则；保存后回读及未提交字段不变，待规则与数据 |
| MD-B05 | 修改/清空邮箱 | 专用测试会员；格式、重复、清空、验证码/审核规则待确认；需核失败无副作用 |
| MD-B06 | 审计及来源 | 已知手机/姓名变更事件，与正式资料及时间对应；历史是否覆盖后台/客户端/KYC修改待确认 |

已向用户集中确认：昵称/性别/邮箱保存后是否立即生效；邮箱是否可清空、重复，是否需验证码/审核。未获答前不自定规则或执行编辑。

## 用户确认（2026-09-17本轮补充）

- 昵称、性别保存后立即生效，不需要审核。MD-B04据此核保存→详情/列表回读一致，不能要求生成KYC复核申请；字符长度、非法枚举和失败无副作用仍待实测。
- 邮箱目前已在客户端取消。退出客户端邮箱功能测试；后台仍可见编辑入口，其是否保留有效业务用途尚未明确，不能把客户端取消直接等同后台接口下架。MD-B05暂缓，不继续索要客户端邮箱验证码等规则。
- 手机号来源于注册时的记录，真实姓名来源于KYC。玩家不能自行修改，后台修改只限KYC复核路径；不设计客户端自由修改或普通member/update修改手机/姓名的正向用例。
- 历史验证应沿注册/KYC正式资料及复核事件取证，不能拿未复核草稿当正式资料变更；初次注册/首次KYC是否生成一条历史、无实际变更是否追加历史仍不能从上述来源规则推出，先核实现证据。既有KYC手机号只读规则保持，不能据“后台限KYC复核”推导复核可以修改手机号。

前面的提问队列为当时状态；昵称/性别生效和资料来源已答复，后续不重复询问。当前编辑测试缺口主要是隔离测试数据与接口边界，非审核业务口径。
