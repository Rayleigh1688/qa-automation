# 管理后台：会员列表

[模块入口](../../README.md) · [问题](questions.md) · [API验证设计](test-cases.md) · [会员详情](../member-detail/README.md)

2026-09-16 17:40—17:42（UTC+8）FAT首轮页面发现，复用已登录后台。范围仅会员管理→会员（`/member-center/list`）。VIP配置、VIP奖励表、VIP签到日志、邀请人转移记录等独立菜单不在本轮；详情独立目录承接。本轮未查询特定个人资料、创建会员、导出、改密码、冻结/解封、转移或转代理；只查看当前列表、打开表单后取消及查看详情入口。当前会话筛选值不当作系统默认值。

## 筛选区

| 分组 | 已观察字段 | 待映射业务/API |
| --- | --- | --- |
| 会员定位 | 手机号/账号/UID三选一；批量查询 | 字段格式、精确/模糊、单值与批量优先级 |
| 身份/状态 | 会员状态、代理资格、账号类型、会员等级、KYC状态、受限状态 | 不把会员、KYC、受限状态混为一个状态；各自枚举与组合规则待核 |
| 关系/来源 | 上级代理手机（带选择器）、邀请人、引流渠道 | 代理上下级与邀请关系区分，切换项全集待补 |
| 时间 | 注册时间/最后登录时间二选一；另有首存、最后存款、最后提款时间范围 | 时区、包含边界、成功状态与时间字段、多个条件交集待核 |
| 风控/数值 | 注册IP（带选择器）、风控等级（带选择器）、存款次数上下界（带选择器） | 选择器全集及范围边界待补，不将默认显示0视为已启用过滤 |
| 操作 | 查询、重置、收起、批量查询 | 重置范围、条件保留、分页回到第一页等待实证 |

## 列表字段与操作

- 21组表头：UID、账号、手机、余额、会员等级、邀请人、代理资格、会员状态、风控等级、风控标签、最近登录、引流渠道、归属门店、在线状态、存款、提款、存提差、GGR、累计投注、活动礼金、操作。
- 余额同时展示玩家钱包与商城代币；会员等级展示VIP与XP。会员状态组同时展示会员、KYC和受限状态。
- 登录信息包括最近登录时间/IP、设备、重复用户数、客户端版本及注册时间/IP；金额列带累计金额、次数、首存/末次充提时间。金额、统计期间、刷新延迟未对账，不能沿用BI口径当作本页规则。
- 列表可见：XP成长日志、邀请人转移、风控等级按钮、IP/设备入口、存款/提现/投注/礼金金额入口；行尾查看明细、解封、风控、重置密码、转为代理。金额/日志入口尚未逐个点击核对目标。
- 页面工具：新增会员、导出、刷新、表格密度、列设置、分页。导出未点击，避免在规则未核时生成包含个人资料的文件。

## 已打开表单

| 功能 | 页面所述字段/规则 | 本轮边界 |
| --- | --- | --- |
| 批量查询 | 手机号或会员UID；文字输入或CSV上传；文字用英文逗号/换行分隔；单次最多2000笔，超过提示中止；取消/套用筛选 | UI说明，未验1999/2000/2001边界、CSV解析、去重或无效项处理 |
| 创建会员 | 账号/邮箱/手机号标必填；昵称、性别、生日、状态、推荐码、是否顶级用户、上级ID、备注、标签；手机号显示+63 | 只打开取消；未验证校验器、身份关系或默认密码生成 |
| 风控操作 | 提示确认风控会员；冻结理由与Google验证码标必填；以后再说/提交验证 | 未提交；限制预期采用下方用户确认，不凭弹窗文案推导状态枚举 |
| 邀请人转移 | 提示被转移人需无下级会员、无邀请人；更新后重新计算好友邀请/里程碑奖励；目标邀请人手机号搜索、必填备注、0/200提示 | 当前界面文案待业务来源核对；未搜索/提交，不证明后台校验及奖励重算正确 |

## API资产与执行边界

[会员接口目录](../../../api/catalog/admin/member.csv)供发现；[后台手册](../../../api/runbooks/ADMIN.md)记录会员列表已验证POST+CBOR，不能直接照目录旧GET执行。手册的实测环境边界保留，本轮未捕获Network或独立调用API。

| 功能 | 已有接口资产 | 尚未明确 |
| --- | --- | --- |
| 列表 | /admin/member/list，优先核对现有POST契约 | 本轮字段、批量/CSV契约、排序/时间单位 |
| 详情 | GET /admin/member/detail | UID关联与字段来源，在详情目录维护 |
| 创建 | POST /admin/member/insert | 必填/唯一性/关系和凭据生成规则 |
| 导出 | GET /admin/member/export | 条件一致性、列范围、任务/权限及数据量边界 |
| 风控/解封 | POST /admin/member/risk/update、/admin/member/unblock | 按钮真实请求与状态流转待映射，不凭命名断言 |
| 关联日志 | GET /admin/member/change/logs、/admin/member/risk/log、/admin/member/log/listv1、/admin/member/log/list | 各入口的参数及数据范围待核 |
| 转移/重置密码/转为代理 | UI已发现 | 尚未确认对应契约；不猜路径、不调用写接口 |

以上为资产基线PARTIAL，不是页面/API验收通过。具体账号、UID、IP和个人字段不写入模块文档。

## 2026-09-16用户确认及状态来源

以下决定优先于上方首次页面发现中的待核说明，不代表已实测：

- 累计存款、提款、GGR、投注、活动礼金均为历史累计；注册时间只筛选会员，不改变累计统计窗口。金额组成/刷新时效仍未验证。
- 手机号、UID搜索优先级最高，不受时间限制，不考虑与其他筛选条件组合。单值及批量用例据此设计；账号搜索未获同样确认，不能扩大例外。异常请求同时传不同身份条件的服务端优先级未定义。
- 批量最多2000笔按输入数量计算，不按去重后数量。非法输入整批拒绝，不跳过坏项继续查询。重复项、空行的解析细节和合法但不命中项的结果尚未确认，不能把“不命中”自动归为非法。
- 邀请人功能当前已下架，邀请人筛选、转移与奖励重算不再作为本轮有效功能验证。此前可见入口/提示仅保留为残留UI观察；不得因此推定上级代理、代理资格或转为代理同时下架。

状态依据：[TLSQ-148 合規相關調整](https://alibaba-international.atlassian.net/browse/TLSQ-148)，2026-09-16读取正文及评论元数据（评论总数0），无子任务/关联单，更新时间2026-06-08 10:11:30 +08:00。正文内嵌图片未视觉读取，未发现外链产品文档；以下只使用文字规则。

| Filbet状态 | 来源说明 | MX映射 |
| --- | --- | --- |
| NORMAL | 默认正常状态 | Active / 300 |
| FROZEN | 注册后72小时未KYC的会员 | Deactivated / 301 |
| RISK | 后台会员详情调整受限状态 | Suspended / 302 |
| Blocked | 玩家在My页面自我封禁 | Suspended / 302 |
| Banned | NDRP | Suspended / 302 |

映射用于MX EF05注册资料接口的current_account_status，以及EF06钱包接口的accounts.player_status。300/301/302是MX对外状态码，不直接当作后台列表state/sub_state或KYC枚举。功能限制现由用户补充确认，见下表；解除条件与72小时触发精确边界未在本轮验证，不影响已明确的限制断言。该单另涉及合规注单门店/厂商ID映射，本轮不扩展到这些接口的执行。


### 会员状态功能限制（2026-09-16用户确认）

| 状态 | 网站访问 | 进入游戏、充值、提现及其他业务操作 |
| --- | --- | --- |
| NORMAL | 允许 | 不受该会员状态限制；仍需满足各功能自身的权限、KYC、余额等前置 |
| FROZEN / RISK / Blocked / Banned | 可访问网址 | 全部禁止，不能仅隐藏按钮而允许API绕过 |

来源：本会话用户明确“除了normal不受影响，其他状态都只能访问网址，不能做任何事情”，并明确列举进入游戏、充值、提现。该规则补充TLSQ-148，适用于上述会员受限状态，不将KYC审核状态或复核状态直接视为同一枚举。网站可访问不表示可以进入游戏或提交业务请求；登录接口具体响应形式、已有会话处理及错误码尚未实测，不自行补写。

后续API断言应覆盖不同受限状态及已有会话：游戏启动不得成功/返回可用启动凭据，充值、提现不得成功建单或引起扣款/冻结等禁止副作用。NORMAL样本只证明不被状态拦截，不豁免其他业务校验。本次仅确认预期，未修改会员状态或执行反例请求。

## 首轮API执行

2026-09-16按用户指令暂缓2000笔边界，完成基础查询/冲突筛选/两页分页/详情映射/无认证拒绝只读核对，见[验证记录](verification-20260916.md)。本页前文“未调用API”是首轮页面盘点边界，最新执行以验证记录为准；未提交任何会员或资金修改。

2026-09-16用户提供浏览器请求后，注册时间实际参数已确认为start_time/end_time（毫秒），page_size=20；请求体CBOR，浏览器Content-Type为application/x-www-form-urlencoded。本轮按此编码组合复验成功；旧reg_*键不再用于注册时间验证。用户页面翻页正常，第二页t=0保留为总数返回约定待核，不标分页功能BUG。
