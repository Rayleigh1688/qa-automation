# 会员列表：后续API验证设计

[功能基线](README.md) · [问题](questions.md)

首次设计基于UI发现；2026-09-16已执行首轮只读API，最新覆盖见[验证记录](verification-20260916.md)，未覆盖子场景仍NOT_RUN，ML-C10为OUT_OF_SCOPE。记录数据/预期缺口，不加入自动门禁。

| ID | 场景 | 预期与数据 |
| --- | --- | --- |
| ML-C01 | 手机/账号/UID查询优先级 | 手机号/UID最高优先级，用注册时间不命中及其他条件不匹配的既有会员验证仍按身份查询，不做条件交集；账号查询单独核契约，不继承时间豁免 |
| ML-C02 | 身份与状态筛选 | 不同会员/KYC/受限/代理/账号类型样本；无手机号/UID时核对普通筛选组合，指定手机号/UID时按ML-C01；状态引用TLSQ-148，不混用MX码与后台枚举 |
| ML-C03 | 时间/次数范围 | 不带手机号/UID时核时间与次数边界、反向、0次、无存取款；注册时间只筛会员，不改变累计金额；身份查询豁免时间见ML-C01 |
| ML-C04 | 批量与CSV | 2000笔及1999/2000/2001边界按用户最新决定DEFERRED，规则保留不执行；混入非法输入整批拒绝，不接受部分成功；手机号/UID优先，不与其他过滤组合；CSV/空行/重复解析与不命中另核 |
| ML-C05 | 排序分页及计数 | 已知全集、跨页无漏重、筛选计数一致，重置与空集按实际契约 |
| ML-C06 | 金额与详情关联 | 钱包/代币分开；存提/GGR/投注/礼金按历史累计核对，切换注册时间不改变同会员累计值；金额组成与源字段待核 |
| ML-C07 | 导出 | 同筛选与权限、列/金额/行数、任务失败及大数据量；未确认契约不生成导出 |
| ML-C08 | 创建会员 | 合成专用资料、必填/唯一性/关系约束；成功只创建一次、失败无残留 |
| ML-C09 | 风控/解封 | 专用账号；FROZEN/RISK/Blocked/Banned仅可访问网站，游戏启动/充值/提现等业务API必须拒绝，无有效游戏启动凭据、无成功建单/扣款/冻结等禁止副作用；覆盖已有会话；NORMAL仍按各功能独立前置验证；状态修改权限/TOTP、审计与解除条件另核 |
| ML-C10 | 邀请人转移 | OUT_OF_SCOPE：2026-09-16用户确认功能下架，停止资格/奖励重算验证，不因可见旧按钮继续执行 |
| ML-C11 | 转为代理/重置密码 | 独立账号、原状态、明确权限与认证流程；状态/会话影响及审计，契约待补 |
| ML-C12 | 日志/明细入口 | 同UID关联与时间范围；列表入口和详情共用查询不重复维护规则 |

首轮覆盖：ML-C01基础身份查询通过，冲突筛选与用户优先级规则不一致；ML-C03旧注册时间参数未生效、有效最后登录时间仍影响身份查询；ML-C05前两页无重叠但第二页t=0待核；ML-C06八项跨接口数值一致，GGR不一致、详情投注字段空。以上均非整体用例PASS。无认证查询拒绝已验证，低权限和写流程未验证。
