# 会员列表：首轮只读API核对

[基线](README.md) · [问题](questions.md) · [用例](test-cases.md)

2026-09-16 18:33—18:35（UTC+8），FAT，按用户“2000个先不用测，其他整理该测的测”执行。每个独立诊断进程fresh登录，复用本地配置会员，只查列表/详情；没有创建、风控、解封、充值、提现、转代理、改密或导出。没有测试1999/2000/2001或进行批量压力请求。未写数据库/提交BUG。

## 方法和证据

- POST /admin/member/list + CBOR；GET /admin/member/detail?uid=...。旧Bruno正文仍写GET，实际POST成功；本轮不重放GET来制造失败。
- 首先手机号精确匹配一个已知会员，从本次响应取UID，再测UID和详情，避免使用列表第一条任意会员。
- [首轮摘要](../../../reports/qa/member-list/20260916/readonly.json)、[字段映射与复核](../../../reports/qa/member-list/20260916/mapped-results.json)只保留脱敏断言和必要金额，无身份、凭据或完整会员响应。
- 两次映射比对因本地Decimal转换遇到非数值中止，分别保留followup.json、followup-complete.json；这属于诊断脚本错误，不计产品失败。最终确认详情bet_amount为空字符串，明确NOT_COMPARABLE，不将空值转换为0。未对写操作重试。
- 页面选择器展开尝试遇只读元素错误，未修改筛选或据此判页面失败。此次接口登录可能影响之前浏览器会话，UI业务结果不与本轮独立API结果混用。

## 查询与权限

| 场景 | 结果 | 判断 |
| --- | --- | --- |
| 手机号/UID基线 | HTTP200、status=true、data对象；均唯一命中同会员 | 本样本通过 |
| 手机号/UID + 相反KYC状态 | 两者均t=0，目标消失 | 与用户定义的身份条件优先存在差异；API仍组合过滤。需核页面是否主动移除冲突参数，尚不归因具体前后端 |
| 旧文档reg_start_time/reg_end_time | 2000年窗口控制组仍返回当代数据 | 参数未证明生效，不能用身份查询仍命中来证明绕过时间；登记契约缺口 |
| last_login_start_time/last_login_end_time | 2000年窗口控制组为空；基线会员叠加手机号/UID亦为空 | 有效时间条件仍影响身份查询。与用户不限时间规则冲突，保留API层候选问题；注册时间实际参数尚未验证 |
| 分页 | page_size=5，前两页各5条且UID无重叠；第一页t=4474，第二页t=0；按1→2→1复核仍4474→0→4474 | 分页数据片段正常，计数契约差异可复现。需确认是否约定只在第一页计算总数，不直接判前端分页坏掉；未遍历全集 |
| 无认证查询 | HTTP200、status=false，无会员数据 | 该无认证拒绝通过，未覆盖低权限角色 |

请求参数依据Bruno资产：KYC状态为字符串；最后登录时间键来自该请求的params定义。不得用合规/cmpl接口文档补后台缺失字段并假称已验证。老接口说明存在state含义与返回示例互相冲突，状态规则仍以用户和TLSQ-148为准，实际字段映射待核。

## 列表与详情对照

来源为同一轮、同UID的API结果；只能证明跨接口一致性，不能替代独立源订单对账或历史累计完整性。

| 指标 | 列表字段 → 详情字段 | 列表值 | 详情值 | 结果 |
| --- | --- | --- | --- | --- |
| 钱包 | balance → balance | 863911.76 | 863911.76 | 一致 |
| 累计存款 | deposit_total → deposit_amount | 43480 | 43480 | 一致 |
| 存款次数 | deposit_count → deposit_count | 41 | 41 | 一致 |
| 累计提款 | withdraw_total → withdrawal_amount | 1189 | 1189 | 一致 |
| 提款次数 | withdraw_count → withdrawal_count | 4 | 4 | 一致 |
| 存提差 | deposit_withdraw_diff → difference | 42291 | 42291 | 一致，且43480−1189=42291 |
| GGR | ggr → ggr | 912533.51 | 0 | 不一致，复核仍存在，待定位来源 |
| 投注金额 | total_bet → bet_amount（文档候选映射） | 781460.73 | 空字符串 | 无法数值比较；需确认详情UI是否另取统计接口，不能直接认定页面显示为空 |
| 活动礼金 | activity_gift → activity_gift | 0 | 0 | 一致（零样本有限） |
| 商城代币 | tokens_balance → tokens_balance | 89261 | 89261 | 一致 |

会员详情文档指向fb_members_balance作为钱包余额来源，先前读取fb_members.balance得0并不足以判定在线环境错误。物理表/缓存链路尚未实查，本轮接口余额一致不等于已完成数据库映射。

## 当前结论与下一步

首轮PARTIAL：已确认基础查询、无认证拒绝及8项跨接口数值一致；发现身份过滤优先级、GGR、分页计数与详情投注字段四类差异/契约问题。保存原始差异，不标会员模块整体通过。

优先定位页面实际查询参数、详情统计来源，以及分页t是否仅首屏有效，再决定修复归属。2000上限按用户决定DEFERRED，邀请人功能OUT_OF_SCOPE。状态拦截需要专用受限账号与有效登录前置：本轮未修改成熟资金号状态，也未发可能成功的充值/提现请求；写功能、导出、完整筛选组合、CSV解析与各详情页签仍未执行，不把前置盘点当测试通过。


## 19:05浏览器参数复核与用户复现

用户提供页面翻页两次正常的反馈，以及两条实际请求。请求体为CBOR（虽然Content-Type标为application/x-www-form-urlencoded），解码参数为start_time=1788969600000、end_time=1789574399999、page_size=20，page依次1/2，即UTC+8的9月10日00:00至9月16日23:59:59.999。未保存/使用用户粘贴的认证信息；本轮fresh登录，用同样业务参数和Content-Type重新编码CBOR只读查询。

[本轮证据](../../../reports/qa/member-list/20260916/browser-params.json)：第一页20条、t=373；第二页20条、t=0；再次第一页20条、t=373。两页UID无重叠，第一页重查集合一致，返回注册时间均在窗口内。第二页t=0不是缺少日期或page_size=5造成；但用户已确认页面翻页正常，因此**不作为已确认分页功能BUG**。目前行为符合“仅首屏返回总数、后续页面沿用”这种可能契约，设计意图仍未经开发确认。

注册时间参数现已补正为start_time/end_time，旧reg_start_time/reg_end_time不再作为可执行参数。用2000-01-01窗口做空集控制组，再带既有测试手机号仍返回空集：接口仍保留时间过滤，页面是否在身份搜索时主动删除日期参数尚未取证，因此优先级问题继续保留为API/页面契约待定位，不将参数修正直接改判通过。

用户此前提供会员详情截图并明确：列表数据正确，详情显示0错误。将GGR及累计投注归为已确认详情统计问题（列表GGR912533.51、累计投注781460.73；详情页面均0），不再仅写“API差异未确认页面”。截图有效投注也为0，但其独立正确数值未核；未将它的预期等同累计投注。用户截图在会话中，未假称已保存本地图片或提交远端BUG。
