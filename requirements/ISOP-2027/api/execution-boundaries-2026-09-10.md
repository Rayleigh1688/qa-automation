# ISOP-2027 FAT边界补测（进行中）

> 留存说明（2026-09-11）：本文是原日期的文字结论。用户已授权删除旧结果、扫描和临时脚本，文内旧路径只表示历史来源，不能作为当前可复核/可导入证据；当前入口与保留范围见[清理记录](../../../docs/project-cleanup-2026-09-11.md)。

用户最新范围：先完成FAT全部可执行API，再UI读写及边界；不扩展UAT。无需用户手动截图，UI由自动化采集脱敏证据。PNG必须支持、最大500KB，执行按500×1024=512000字节核对，并保留500000附近观测。原因最大长度仍未明确。BUG提交前确认与最终一次性群清单约定不变。

本文件是进行中检查点，不代表API已全测或UI已完成。初轮证据见[API读写](execution-rw-2026-09-10.md)。权限撤销和恢复见[权限实测](execution-permissions-2026-09-10.md)，已恢复用户指定的Codex角色配置；没有BUG创建或群投递。

## 已补执行

- 原第二专用会员通过受控KYC审核准备为状态5；8项文字编辑、双人核准、三份正式详情及其他字段/状态保持通过。证据：`results/rw-20260910-images/`的boundaries.py和result.json。
- KYC上传PNG 499999、500000、511999、512000字节均接受；512001字节与36字节损坏PNG也接受，后两项为异常。下载本轮对象核对，与上传原字节完全一致，不是压缩后达标。证据：同目录png-boundaries/results.json、storage-check.json。
- 将超限PNG用于原第二会员编辑也被接受，新增待复核申请及审计，未核准该异常申请。
- 第三个独立会员通过初次KYC正常驳回到状态3；该状态编辑及双人核准通过。证据：`results/boundary-20260910-resubmit/`。
- 随后第三会员A/B并发编辑两请求均成功，生成两条待复核和两条审计，违反唯一待复核规则；停止后续并发复核，保留现场。
- 第四独立会员执行输入边界：编辑和复核缺失/无效认证共4组合均拒绝，无资料/申请/审计副作用。`review_status=-1`却被接受，申请从待复核与已处理列表同时消失并追加审计；后续0/4、自审伪造及纯空白原因未执行，不能算通过。证据：`results/boundary-20260910-inputs/`。

## 尚在处理

状态1/4及页面名称映射、其他参数与只读字段、分行同步、损坏图片用于编辑、跨不同复核人的并发矩阵、UI读写/图片边界。第三复核身份和故障注入接口尚未具备，不能伪造跨三身份并发或故障恢复通过。

前端只读契约检查位于`reports/telegram/runs/isop2027-full-20260910/inspection.json`：已确认页面/kyc及四个可见状态选项，正在核对实际请求值；这是定位和契约准备，不是UI读写验收。

各异常样本不得被后续成功接口覆盖；新增测试使用独立样本。已有evidence-manifest.json属于其生成时间点，补测后文件可能变化，最终应另生成本轮最新摘要而不把旧摘要伪装为最新。

## 后续独立样本补测

- `results/boundary-20260910-review-race/`：B（Codex角色）创建唯一草稿，A对同一申请同时发送核准/驳回。两请求均成功，待复核1→0、已处理0→1、审计1→3，唯一胜者保护失败，停止该链并保留现场。这是同一有权复核人的并发请求，不代表已测两个独立复核身份。
- `results/boundary-20260910-whitespace/`：B创建独立草稿，A以空格和制表符驳回被接受，待复核1→0、已处理0→1、审计1→2。原“接口明确返回业务拒绝”是失败断言的预期文字，实际为接受；与空原因候选合并整理。
- 上述两个新会员单UID批量详情请求再次返回额外记录，契约异常保留；其他断言仅使用唯一精确UID行和单会员详情，未使用额外记录执行写操作。
- 用户确认Jira端BUG状态变更脚本已全面通过，不再检查。部署版本未提供不阻塞本轮FAT测试，记录环境、时间与证据。普通人工审核测试图片即可，不要求用户提供真实证件或截图。

- `results/boundary-20260910-corrupt/`：36字节非PNG内容、声明image/png的对象用于编辑被接受，新增一条申请及审计；未核准。
- `results/boundary-20260910-readonly/`：篡改phone、user_message、kyc_status后提交；草稿after_info中的手机号和留言匹配篡改值，kyc_status保持原值。正式资料仍未生效，异常草稿未核准，不能宣称已证明正式手机号被修改。protected-fields-check.json记录脱敏比较结果。
- UI定位已解除状态下拉框遮挡Query按钮的问题。状态控件为多选、默认选中Under Review；初步请求组合为2,0／2,3／2,5，不能把组合值当单项映射。后续筛选必须先清空默认值。
- 已独立准备UI会员：`results/ui-fixture-20260910/`；API准备不计入UI通过。UI过程证据仍位于`reports/telegram/runs/isop2027-full-20260910/`。

## 分行补测停止点

在同一独立UI会员完成正常驳回后，B提交nearest_branch为另一独立门店列表名称，并传sid=3。接口接受；草稿名称更新，但after_info.sid仍为2。受控断言失败后停止，未核准。只读数据库前/待复核快照均显示fat.fb_members.sid=2、fat.fb_members_kyc.sid=2，正式分行名称不变。

复查Bruno `后台/kyc/ekyc编辑.bru`的docs块，只声明nearest_branch，未声明sid；故“草稿sid未更新”暂为契约待核对，不直接计作已确认PRODUCT BUG。证据：`results/ui-fixture-20260910/branch.py`、private-state.json、branch-db-evidence.json。当前保留1条分行草稿、2条已处理UI申请；不能用继续核准来制造分行同步通过。

## 当前可查阅入口

- [候选BUG清单](BUG-review-2026-09-10.md)：8项API候选、1项UI候选及单列契约/配置观察，均未提交。
- [UI实测](../ui/execution-2026-09-10.md)：姓名核准、三图文字驳回、只读/空原因/图片边界/已处理详情等实测，不代表UI完整矩阵通过。

## 身份与筛选增量

- permissions-20260910/spoof-self.py：A对自己创建的唯一申请夹带B的reviewer_id/updated_name，核准和驳回均拒绝；正式资料、申请、审计完全不变。仅证明列出的身份篡改请求被挡，不把未列字段视为全测。
- ui-fixture-20260910/query-matrix.py：14项电话、姓名、提交人、毫秒时间窗及组合命中/不命中检查均匹配已知两条已处理申请集合和总数。
- 第15项越界分页（第3页、每页1条、总数2）返回d=null、t=2，触发本地数组契约检查；query-finalize.py单独记录为CONTRACT，并核对整个查询矩阵前后正式资料、申请及审计无变化。不把该空值形状异常归为已确认PRODUCT BUG。
