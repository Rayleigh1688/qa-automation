# ISOP-2027 FAT 候选 BUG 确认清单（持续补充，未提交）

测试负责人：Davinci。以下均未获具体BUG建单确认，没有创建Jira单或发送群消息。部署版本未提供，API文字结论来自2026-09-10 FAT；其旧原始结果已于2026-09-11清理，B01—B08均须补当前版本复现证据后再提单。表内路径仅记录旧来源，不可再打开或导入。API和UI结论分别记录，不能互相覆盖。

| 候选 | 实际现象与影响 | 本轮证据 | 限制 |
| --- | --- | --- | --- |
| B01 驳回原因服务端未校验 | 空字符串、纯空格/制表符均可驳回，申请转已驳回并追加审计 | `results/rw-20260910/`、`results/boundary-20260910-whitespace/` | UI空原因拦截通过，不能判UI同样失败 |
| B02 图片大小服务端未校验 | 512001字节PNG上传成功并可生成编辑申请；下载仍为原始超限字节 | `results/rw-20260910-images/` | UI对512001拦截、512000上传通过；未核准超限草稿 |
| B03 损坏图片被接受 | 36字节非PNG文件声明image/png，上传和编辑均接受 | `results/boundary-20260910-corrupt/` | 不涉及证件内容真实性；未核准草稿 |
| B04 并发编辑产生多条待复核 | A/B同时编辑同会员均成功，产生两申请和两审计 | `results/boundary-20260910-resubmit/` | 两条异常申请保留，不继续复核 |
| B05 并发核准/驳回均成功 | 对唯一申请同时核准、驳回均返回成功，追加两条复核审计 | `results/boundary-20260910-review-race/` | 同一有权复核人两并发请求；非三身份矩阵 |
| B06 非法复核状态被接受 | review_status=-1被接受，申请从待复核和已处理列表同时消失 | `results/boundary-20260910-inputs/` | 0/4等变体未因此判通过 |
| B07 普通角色缺少复核权限节点 | Codex已有可配置KYC权限，但新增复核列表/提交均permission；API树和只读权限表缺少对应节点 | `results/permissions-20260910/` | 原Codex权限已恢复；未插入权限表 |
| B08 只读字段可写入草稿 | API提交篡改phone和user_message，两字段进入after_info；kyc_status篡改被忽略 | `results/boundary-20260910-readonly/` | 未核准异常草稿；尚不声称正式手机号已被修改。UI三控件禁用通过 |

## UI候选

B09：编辑表单替换图片后，“Restore”按钮被相邻“Replace photo”按钮遮挡，普通鼠标点击超时。1365×900视口、1000像素编辑弹窗实测；按钮存在、启用且可见，Playwright命中测试明确报告相邻按钮拦截pointer events。未force点击、未提交该表单。2026-09-11固定UI整批再次观察到遮挡，当前证据为[局部截图](../../../reports/qa/ISOP-2027/20260911T050501Z-d626392b/2027-UI-014-restore-click-target.png)及同目录ui-diagnostics.jsonl；旧调试脚本/截图已删除。其他视口尚未证明同样受影响。

## 仍需区分契约或补充证据

- 空结果/越界分页返回d=null（越界页t仍为真实总数2），与本地空数组契约不一致；保留CONTRACT，不据此创建PRODUCT BUG。


- 无业务字段变化、仅临时图片URL不同也创建申请：保留API候选；UI无变更提交正确拦截。需明确服务端是否应按稳定字段去重。
- `/admin/kyc/details`传单UID返回额外行：多个独立会员重复出现。只使用唯一精确UID行核对，不访问或写入额外行；接口查询契约与额外行来源待确认。
- Codex角色编辑页门店列表`/admin/shop/gs/list`返回permission，影响分行选项；需区分权限配置与新增功能漏配，不与复核节点缺失无条件合并。

当前转为API自动与UI人工分工，尚未进行本批人工回填，本清单不是全量验收完成声明。完成整理后由用户确认具体条目，再整批建BUG单并一次性发群。Jira状态变更通知按用户确认已全面通过，不在本轮重测。
