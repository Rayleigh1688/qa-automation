# ISOP-2022：问题与决定

[测试设计](design.md) · [独立测试用例](test-cases.md)

保留首轮表单评审和用户答复历史。Q/D编号不变；下文数据库证据均指 [设计中的只读核对记录](design.md#数据库只读核对记录2026-09-07)。后续答复记录日期、决定及来源，再同步验收规则和用例。

## 逐表单评审

以下是需求文档问题和验收缺口，不是已复现的系统缺陷。截图是 2026-09-02 的需求附件，不是本轮测试环境证据。

| 位置/截图时间 | 已读取的表单内容 | 不一致或需补充内容 |
| --- | --- | --- |
| §1 仪表盘，正文表格 | 4 类 GGR、有效投注、总 GGR | 汇总行“偏移 1 日”容易被实现为对体育也过滤；应写非体育与体育分别求和。各游戏分类与后台场馆分类的对应关系未提供 |
| §2 22:43:53 | 结算时间、注单状态/类型、投注金额；列表可见投注时间；底部注单数/总投注/有效投注/派彩/GGR | 体育分支已由用户确认按游戏类型切换原表/独立体育表，明细与小计合计同范围；列表结算时间列是否存在于横向隐藏区域未证实；应明确结算列和排序、汇总是否同步变更 |
| §3 22:46:10 | 日统计有个人统计、邀请统计；FS/JP 投注与 GGR、总彩金、总佣金；存提款指标、导出、重置数据 | 仅列六个受影响字段，FS/JP独立注单类型及UID人数去重已确认；邀请好友已取消，邀请统计移出范围；佣金不因此扩展需求；重置数据究竟是筛选重置还是业务重算需辨别 |
| §4 22:52:11 | 游戏类型、统计时间；注单数、总投注、排除投注、有效投注、总派彩、GGR、FS/JP | 未列完整影响字段；日统计“有效流水”与这里“有效投注额”是否同义未定义；时间归属会影响注单数 |
| §5 22:53:39 | 游戏报表截图圈出三份报表菜单；有线路、投注人数、注单数、Free Spin、系数、导出、逐行重算 | 只有游戏报表的实际字段截图，不能据此推定另两份字段完全相同；“应仅需调整物化视图”已由用户补充为RisingWave按新逻辑整体重跑；刷新/重算完成证据在提测阶段核对 |
| §6 22:56:02 | 会员日统计截图圈出两菜单；层级/邀请人筛选、总人数/达标人数、小计/总计、导出、详情 | 会员游戏日统计未独立展示；邀请好友相关验证移出范围；详情与列表/导出同步范围仍需页面核对；非投注指标如何保持原时间轴未说明 |
| §7 23:11:27 | 平台与体育等分类页签；投注时间选中；红字提示；日期为 08-01 00:00:00 至 09-02 23:59:59 | 覆盖 33 个自然日与“已限定31天”不一致；截图在投注时间下显示结算模式提示，且红字写“选择异动时间时”，正文写结算时间；标注图应更新，不能当目标 UI 直接验收 |
| §8 23:39:07 | 菜单为“投注记录”；游戏类型、注单类型两个独立筛选器；日期；合计行 | 正文“投注管理”与菜单名称不同；原文体育被写为注单类型，用户已确认应为游戏类型；线路、投注/结算时间、时区均为新增筛选能力，字段对应见下方确认记录 |

## 待确认事项与修订建议

优先级表示对本需求验收的阻塞程度，不表示已进入仓库 P0。

| ID | 级别/性质 | 问题与建议 | 影响 |
| --- | --- | --- | --- |
| D-01 | 已解决/用户确认 | 2026-09-07用户确认：体育属于游戏类型，与slot、fish、bingo并列；注单类型是独立条件。按此修正本地验收点与用例；Jira原文未修改 | AC-07、体育包含/排除 |
| D-02 | 部分澄清/图文冲突 | 用户已确认投注检索向两侧扩1个自然日，结算范围不变；这是内部检索范围，不代表可选择33天结算范围。§7截图33个自然日示例与31天限制仍冲突；投注时间下却画结算排除提示，且提示写“异动时间”。替换为符合正文的目标截图，标清旧图/示意图 | AC-06/09、UI基准 |
| D-03 | 低/命名及笔误 | §8 页面标题“投注管理”与截图“投注记录”需统一；技术限制“另行已结算时间纪录”建议改“另行以结算时间记录”并明确新表语义 | 页面定位、可读性 |
| Q-01 | 已解决/用户确认 | 结算查询默认从原注单表查询非体育类型，明细及小计/合计排除体育；选游戏类型体育后改查独立体育表，按结算时间筛选，支持早球等长周期。明细默认非体育，与包含体育的统计页不能无条件比总额 | AC-03、会员对账 |
| Q-02 | 本阶段范围已定/已知限制 | 用户决定先按非体育前后最多各1个自然日实现，窗口外遗漏可作为本阶段已知限制，后续减少偏差；窗口内应有记录的遗漏、重复或计算错误仍是缺陷。补单是用户提出的可能兜底，部分场馆不支持；是否实施、如何回补及实际效果尚未验证，不作为本版必选通过前置 | AC-01/04/05；记录窗口外笔数/金额偏差，不宣称全量无漏单 |
| Q-03 | 已确认/实现待核对 | 毫秒精度、含端点；整日00:00:00.000—23:59:59.999，手动时间段保留实际起止。按选择的整数时区转换时间戳；不把10分钟查询扩成整日。默认/重置行为在UI阶段核对 | 时间边界及短区间用例可确定 |
| Q-04 | 业务规则已确认/枚举待核对 | 部分结算、取消、作废、退款均不计入结算统计；重结算使用最新注单数据及最新结算时间。实际状态枚举、更新/去重键在源表与接口核对 | 状态预期已定 |
| Q-05 | 主要口径已确认/报表映射待核对 | 总投注含有效无效，有效取validbet；用户确认settleamount=派彩−投注，故同一口径下派彩=settleamount+投注。活动彩金来自独立活动彩金表，不关联注单；免费旋转奖励按注单独立类型统计；FS/JP独立类型、人数UID去重。邀请好友已取消。已按用户截图定位orders.tbl_game_record，实际输赢字段为net_amount、有效投注为valid_bet_amount；不将报表settle_amount误认原始输赢字段；GGR映射/活动彩金时间字段仍待实查 | 不再追问已给出的盈利公式；数据库证据边界见设计文档 |
| Q-06 | 方案已确认/执行待核对 | 用户确认物化视图为RisingWave，按新增偏移逻辑整体重跑；用跨天样本的源表独立计算结果对总报表。提测时记录重跑版本、实际覆盖范围/完成标识及同步等待条件；不再追问是否全量重跑 | AC-05；本轮未重跑或造数 |
| Q-07 | 映射已确认/交互待核对 | 用户已确认当前1线仅node=1、2线为node=0或2，0为历史未分线路默认值。线路归属不再阻塞验收；筛选器默认选中项、是否有全部选项及非法参数处理留待契约/UI核对 | AC-08、跨报表线路一致性 |
| Q-08 | 已确认/实现待核对 | 时区-12至+12，步长1小时，默认+8；本需求不提供半小时选项。按手动选定的本地起止时间和时区自然换算毫秒时间戳，支持10分钟至合法月范围，不强制整日 | AC-08、UI/API时间换算 |
| Q-09 | 已确认/展示待核对 | 小计是当前页，合计是全部筛选结果；前端最多31天。内部投注时间向前后各偏移1天，并且结算时间在原选择区间内。空结果0/横线等展示在页面核对，不再阻塞汇总定义 | AC-09 |
| Q-10 | 中/交付前置缺失 | FAT部署版本、新体育接口/表、回填结果、稳定样本与刷新SLA、受限角色均未提供；拥有管理员权限不等于已具备越权测试账号 | 执行准备 |

D-01已解决，偏移单位和两层时间筛选已确认；Q-01查询分支已解决，Q-02按本阶段限制处理；Q-03/Q-08时间换算、Q-09汇总定义及Q-06整体重跑方案已确认；Q-04排除状态及Q-05盈利公式也已确认；原始注单表及基础字段/枚举已实查；后续集中核对体育独立表、报表映射与提测契约；其余不依赖答案的表单检查、现有接口定位及样本盘点可继续。未确认的业务预期保持 BLOCKED_EXPECTATION，不能用当前接口输出倒推验收答案。


## 用户确认记录（2026-09-07）

来源：本会话用户澄清，优先于首轮评审中的未决推测；早期字段称呼来自用户；后续已按用户截图定位 `orders.tbl_game_record`，实际列名以“原始注单表核对”记录为准。HTTP参数名仍需提测契约核对，不能直接当成请求参数。

- 体育属于游戏类型，与 slot、fish、bingo 并列；与“注单类型”分开。
- 在现有筛选条件上新增线路条件，对应注单表 `Node`，不是替换现有条件。
- 用户进一步确认：1线为 `node=1`；2线为 `node IN (0,2)`。`node=0` 是历史没有区分线路时的默认值，查询时归入2线；这不表示筛选器默认选择2线，也不要求修改历史记录的node值。
- §8新增投注时间和结算时间两种时间筛选方式，分别对应 `creattime`、`settletime`；不能误实现为用户必须同时填写两个独立区间。
- 新增时区选项；范围和默认值沿用需求正文 -12 至 +12、默认 +8。具体交互与请求换算在接口/UI阶段核对。
- 非体育结算查询：投注时间向前后各扩1个自然日作为检索范围，再与原始结算区间取交集。查询昨天时，投注候选覆盖前天、昨天、今天，最终只返回结算在昨天且满足其他条件的记录。

用户给定日期示例（日期均含首尾日）：

| 条件 | 日期范围 | 作用 |
| --- | --- | --- |
| 用户选择结算日期 | 8/1—8/31 | 最终业务归属区间 |
| 内部投注时间 `creattime` 检索 | 7/31—9/1 | 扩大候选扫描范围 |
| 内部结算时间 `settletime` 限定 | 8/1—8/31 | 排除结算在原区间外的候选 |

两个内部条件为 AND，同时叠加会员、游戏类型、线路等筛选。8/1—8/31的31个自然日查询可能扫描33个投注自然日，内部扩窗不能触发用户查询31天限制。按用户补充的毫秒精度与含首尾日边界，投注范围为 `[7/31 00:00:00.000, 9/1 23:59:59.999]`，结算范围为 `[8/1 00:00:00.000, 8/31 23:59:59.999]`，两者均含端点。

验收样本：7/31投注、8/1结算应纳入；8/31投注、9/1结算应排除；7/30投注、8/1结算无法被此投注窗口命中，作为Q-02本阶段接受的窗口外覆盖限制记录偏差。体育仍按专用结算查询规则，不套非体育扩窗。

### 时间边界补充：Q-03

2026-09-07用户确认本需求时间戳范围使用毫秒；选择完整日期时，起始日期为 `00:00:00.000`，结束日期为 `23:59:59.999`，按包含首尾端点的闭区间验收。最小精度为1毫秒，不能把结束时间截断到 `23:59:59.000`，也不能将次日 `00:00:00.000` 纳入前一天。用户后续明确也支持手动选择10分钟等非整日区间，此时保留实际选择的起止时刻，不强制扩成全天；投注检索按所选起止前后各偏移1天，结算范围保持原值。HTTP参数名称与各页面序列化形式在提测时核对，不据此修改不属于本需求的既有接口契约。

### 本阶段范围补充：Q-01 / Q-02

用户进一步确认先交付一版，再逐步减少偏差：非体育投注检索最多前后各1个自然日，窗口外延迟结算可能遗漏，本阶段接受该覆盖限制。补单仅是可能的后续兜底，场馆支持情况和能否进入查询窗口/更新统计需另行验证；不能承诺补单必然消除差额，也不为获得通过自动补单。

结算查询分支：未指定游戏类型时，原注单表返回非体育明细及对应小计/合计；选体育时改查独立体育表，以结算时间限定。体育存在早球等长周期，不能套用非体育1日投注窗口。此澄清解决会员明细Q-01，不改变仪表盘及统计报表明确要求合并体育的规则，也不新增未确认的会员页提示文案。

测试结论分开记录“约定窗口内的正确性”和“窗口外的覆盖偏差”。前者遗漏/重复/金额错误仍判失败；后者单列已知限制，记录已知样本的笔数与金额差额，不设未经确认的偏差容忍百分比。没有独立完整数据源时注明无法量化总体偏差，不能用少量样本宣称全场馆漏单率。

### 状态、指标与重跑补充：Q-04—09

2026-09-07用户进一步确认：

- 部分结算不计入已完成结算统计。重结算按最新注单数据及最新settletime统计；测试分别覆盖同日金额变化、跨日归属迁移，旧贡献不能残留。
- 总投注额包含有效与无效，有效投注额直接取 `validbet`。派彩不含本金；库中没有派彩字段，用户称盈利字段为 `settle amount`。后续用户已明确原始字段名为 `settleamount`，计算为派彩减投注；同一口径下派彩=settleamount+投注。该公式按用户确认作为依据；旧的“派彩不含本金”描述与常用术语可能不同，测试以实际字段及该公式为准，不根据术语另推公式。GGR页面字段映射仍需实查。
- 彩金为独立活动彩金，与注单无关；免费旋转彩金在注单中有独立类型，FS、JP有独立注单类型；人数按UID去重，同一UID多注或跨游戏不能在总体人数中重复累计。
- RisingWave按新偏移逻辑整体重跑。用跨天测试样本、历史样本的源表独立聚合结果对总报表，并保留重跑完成与数据版本证据。此为后续验证方案，本轮不创建注单或触发重跑；造数沿用隔离测试流程，数据库仅只读。
- 时区为整数小时选项。手选10分钟或1个月等合法区间时，按所选时区转换实际起止时间戳；非体育投注范围为所选S前移1天至E后移1天，结算仍为S至E，两条件同时满足。
- 前端最多选择31天；小计当前页，合计全筛选结果。内部扩窗是查询实现，不能因此把前端31天上限放宽或把短区间扩为整日结算。

最小对账样本组合：跨日非体育纳入/排除、长周期体育、部分结算→完成、重结算跨日迁移、node=0/1/2、同UID多注、有效/无效投注、FS/JP及独立活动彩金、10分钟毫秒边界。金额以源表样本独立计算，不复制RisingWave生成逻辑作为唯一预期；测试数据不足记为数据阻塞。

