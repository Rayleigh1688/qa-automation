# 2026-09-10 维护整理前交接快照

> 留存说明（2026-09-11）：本文是原日期的文字结论。用户已授权删除旧结果、扫描和临时脚本，文内旧路径只表示历史来源，不能作为当前可复核/可导入证据；当前入口与保留范围见[清理记录](../project-cleanup-2026-09-11.md)。

来源：本轮整理前的根目录AI-HANDOFF.md；保留全部原文及历史日期，仅调整相对链接。原文SHA256：`a3867d2761058d552e17c99ad0f6b501108d833add10284a9ba1b065e7d7bc41`。以下阶段决定可能已被后续决定取代，当前入口见[当前交接](../../AI-HANDOFF.md)。

---

# 当前交接

Telegram 当前续接入口：[本轮流程与下一步](../../docs/telegram-next-session.md)。该文件优先于下方旧Telegram历史约定。

2026-09-10后续：用户放开2027只读限制，先复用P0账号/素材/注册与KYC能力准备编辑及双人复核；具体写入仍保留测试前确认。本地目前仅核实一个后台身份配置，第二有权管理员及专项逻辑待补；详见续接入口，未因此执行新业务写入。

2026-09-10 Telegram最新决定：测试过程/报告/候选只留本机；确认具体BUG后完成整批Jira建单及Story关联，再一次性向测试群发BUG链接清单。每Story唯一负责人，ISOP-2027为Davinci。2027本轮API5/6，UI首次4/6、两条脚本修正后定向复测2/2；仅剩空列表CONTRACT差异，无已确认PRODUCT BUG、未建单。完整证据和后续边界见续接入口。

更新日期：2026-09-08。优化起点：`e85cb82`（UI自动化脱离AI，全面通过）。当前主线为 UI P0 受控业务链路；本轮联网结果与停止点见文末，下面历史表不代表本轮通过率。

## 当前结论

P0 已有确定性 API/UI 执行入口。用例范围以 [P0 资产](../../api/p0/README.md) 和默认 UI 清单为准；命令及写入边界见 [命令说明](../../docs/commands.md)，环境规则见 [环境手册](../../api/runbooks/ENVIRONMENTS.md)。

| 最近已记录的联网证据 | 日期 | 结论与限制 |
| --- | --- | --- |
| FAT API 独立组合 | 2026-09-04 | 90/90；包含后台清流，不含真实三方投注 |
| FAT 默认 UI | 2026-09-04 | 固定 10 条全部通过；条款 Case 已移出范围，并非缺陷修复 |
| FAT 完整组合 | 2026-09-04 19:07 | 充值 1200、单注 100 投注 10 次、流水 1800→900，后台清零后提现 1000；关联核对 10/10，主流程 8/8；订单为 under_review，未证明最终出款 |
| UAT 完整组合 | 2026-09-02 | 主流程 8/8、核对 10/10；旧 UI 为 10/11，当前固定套件尚未因此在 UAT 重验 |

以上为历史交接记载，不是本次重跑结果；原始结果路径会被下一次运行覆盖。详细历史见 [冻结交接](../../docs/history/handoff-2026-09-04.md)；原阶段成果报告已删除，不再作为导航入口。

## 有效范围与例外

- 完整主流程包含真实 UI 投注；纯 API 后台清流不能替代投注证据。
- 当前提现阶段接受待审订单及前后台关联一致；真实出款增强验证见环境手册。
- 条款未勾选仍可登录只保留历史产品问题，不自动恢复到默认套件。
- 接口发现保留冻结快照，入口见 [发现索引](../../archive/interface-scans/README.md)。
- CI 尚未随最新本地入口验收，差异见 [CI 状态](../../docs/ci.md)。

## 已完成的工程治理

- README 保留 API P0、默认 UI P0 两个日常入口及报告/用例位置，详细专项命令进入 `docs/commands.md`。
- 公共编解码与请求契约进入 `scripts/qa_core/`，旧入口保留兼容；修正提现待审毫秒窗口、JSON 被误当 CBOR 解码的问题。
- UI 默认报告增加本轮页面、Network 摘要和截图，保留固定用例统计；过期证据不用于补齐。
- 七个扫描相关目录迁入 `archive/interface-scans/2026-09-07/`，统一从 [归档索引](../../archive/interface-scans/README.md) 阅读。共移动 248 份文件；235 份非忽略文件记录迁移前后哈希，13 份本地文件保留忽略属性。仅修正必要 Markdown 链接，历史脚本与数据未改写。
- 新需求入口与设计模板位于 [requirements/README.md](../../requirements/README.md)，不以补齐 P1 或全量接口为前置。

## 最新验证与限制

2026-09-07 `npm run check` 通过：38 份当前文档导航、235 份归档文件校验、API 资产、119 份源码语法及 115 条单元测试。原 P0 代码无扫描目录依赖，日常 npm 命令及结果路径不变。归档脚本保留历史 cwd/输出路径，不承诺直接重跑兼容。

此前用户反馈 API 检查正常；UI 报告用已有结果重渲染并完成桌面/手机视觉检查。公共模块迁移及本次归档后仅执行本地检查，未因此联网或执行资金操作，不能声称最新代码已通过联网验收。CI 也尚未随最新版验收。

改动仍留工作区，未自动提交。历史扫描只供参考，不恢复全面扫描或批量补低风险接口。

## 下一步：新需求 AI 自动化测试设计

2026-09-07 已选择 [ISOP-2022：管理后台统计数据时间调整](../../requirements/ISOP-2022/design.md) 作为试点。基于 Jira 正文、7 张附件截图及3个子任务完成首轮逐表单评审、验收点/候选接口映射和30条场景设计；均未业务执行。本次开始时工作区干净，基线为 `cdb94a1`。

体育会员明细查询方式及非体育1日扩窗阶段范围已由后续用户澄清（见下）；时间精度与端点也已确认（见下）；优先补齐时区交互、结算状态与指标公式、历史回填与刷新约定；正文与截图的分类字段及31天示例冲突详见设计文档。子任务读取时无补充说明；未将待办状态当作未部署证据。

下一步按 FAT 逐页核对当前请求和部署版本，盘点跨日/长周期/延迟结算样本，再实现需求专项。此次未登录业务后台、未执行业务API/UI或资金/重算操作、未更改Jira。现有P0门禁不变；不因需求扩展全面历史扫描。

本轮仅改文档，`npm run check:docs` 通过（39份）；需求设计与交接修改未提交。

2026-09-07 后续整理：需求固定按 Jira 编号组织、不按年月套目录；入口新增需求索引，阶段信息只在需求设计内维护。新增 [新需求测试流程建议](../../requirements/workflow.md)，区分需求评审、后端接口测试、前端联调与回归；模板补充阶段、提测版本、契约及独立数据预期。本次保留此前 ISOP-2022 工作区修改，仅优化文档，未新增执行器或执行业务测试。

后续整理验证：`npm run check:docs` 通过（40份），`git diff --check` 通过；未执行联网门禁，未提交。

2026-09-07 用户澄清已同步至ISOP-2022设计：D-01已解决（体育为游戏类型）；新增线路对应 `Node`，投注/结算筛选对应 `creattime` / `settletime`，时区也是新增。偏移已确定为前后各1个自然日的投注候选范围，最终仍以原结算范围限定；补充8/1—8/31示例及边界样本。D-02原截图冲突与超过扩窗的长延迟风险仍保留，不重复询问已确认规则。仅改文档，业务用例仍NOT_RUN。

2026-09-07 线路补充确认：1线仅node=1；2线为node=0、2；0是历史未分线路数据的默认值。已更新ISOP-2022的AC-08、Q-07及R13/R14，线路归属不再阻塞验收；重点验证历史0数据不漏不重、1线隔离及列表/汇总同条件。筛选器默认选中项不由数据默认值推定。本次仍仅改文档，未执行业务测试。

2026-09-07 Q-01/Q-02澄清：结算查询默认原表排除体育，明细及小计合计同范围；明确选体育则查独立体育表，按结算时间筛选且不受投注1日窗限制。非体育本版最多前后各1个自然日，窗口外漏单作为已知限制观测，不再作为完整兜底交付阻塞；窗口内错误仍判缺陷。补单仅为可能方案，部分场馆不支持，未验证或承诺补单效果。已同步AC-03、问题状态、R06/R09及停止条件；未执行业务测试。

2026-09-07 Q-03补充：用户确认本需求毫秒时间戳，日期范围从00:00:00.000到23:59:59.999，按含端点闭区间验收。已同步设计中的日期示例、公共时间定义、R04/R05，移除秒/毫秒及端点未决项；时区交互另待核对。仅改本需求文档与交接，不变更既有接口时间单位。

2026-09-07 Q-04—09进一步确认已同步需求设计与用例：部分结算排除，重结算用最新数据/结算时间；总投注含有效无效，有效取validbet，派彩不含本金但需核对盈利字段settle amount到报表的推导，彩金独立表、FS/JP独立类型、人数UID去重。RisingWave按新逻辑整体重跑，源表独立计算对总报表；整数时区按手选实际区间转换，10分钟不扩为全天；前端31天上限、小计当前页/合计全部结果已明确。剩余重点是其他状态、派生公式和提测契约/数据准备，不重复询问已确认规则。本轮仅改文档，未造数或重跑。

2026-09-07 用户确认settleamount=派彩−投注、活动彩金与注单无关（免费旋转按注单类型）、取消/作废/退款排除、邀请好友取消。设计已同步，R22标OUT_OF_SCOPE，其余29条未执行。按用户要求只读查询FAT MySQL字段元数据：可见schema未找到settleamount/validbet/creattime；仅定位报表settle_amount，不能代替原表证据。下一步需定位原始注单数据源/表名或本地连接配置，不重复询问已确认业务公式；本轮无业务写入。

2026-09-07 用户截图已定位 `orders.tbl_game_record`，无需再询问原始表位置。直接只读字段查询成功，实际列为bet_amount、valid_bet_amount、net_amount、bet_time、settle_time、node、bet_type、state；早期settleamount等是口头称呼，并非原表列名。抽取prefix=fat的8条非敏感金额样本，时间为毫秒；派彩按用户公式net_amount+bet_amount推导，尚未独立对账。详细字段/枚举证据见需求设计；无业务写入，体育独立表仍待定位。

## 最新交接：ISOP-2022之后的新Story批次评审

2026-09-07 已通过Jira连接器读取创建时间晚于ISOP-2022的全部11个Story（本次快照，最新ISOP-2070，查询无后续页）及23个关联子任务；范围、来源边界、问题优先级和组员分工建议见 [批次评审](../../requirements/review-summary.md)。已读取相关Lark、游戏页Figma画布及KYC/合规列表原型；未逐张核对其余全部附件，动画视频未逐帧验证。

按用户要求每个Jira固定拆分设计、问题/答复、独立用例三份文件，入口见 [需求索引](../../requirements/README.md)。ISOP-2022原用户决定及R编号保留，执行证据迁入 [原专项用例](../../requirements/ISOP-2022/test-cases.md)；模板与流程同步。先保留已确认规则，只让未决答案阻塞关联断言；重点先确认JP拆分后的有效派彩、返利多规则/业务日，以及客户端版本冲突。文档负责人栏待分派，未向组员发消息或操作Jira。

本轮仅新增/整理需求文档，没有执行业务API/UI测试、造数、资金操作、数据库写入或历史重算；设计不等于自动化脚本已实现。已有工作区修改保留，未提交。

验证：新需求验收点均有用例映射，Case ID唯一、Q引用有效、表格列数一致；ISOP-2022原30个编号完整。`npm run check:docs` 未全库通过：检查期间工作区出现原阶段成果报告删除，README.md与本文件历史部分各有一处链接指向该缺失文件；未擅自恢复或覆盖该删除。本次requirements文档导航/npm命令范围检查通过（42份）；`git diff --check`通过。此处不将以前通过记录当作当前检查结果。

2026-09-07 ISOP-2027补充：用户确认待审核时不能提交新的KYC，按Q-01上下文同步后台编辑待复核期间禁止会员新提交/补件；Q-01会员提交问题已解决，AC-04及C15改为前后端拦截和无新增/覆盖副作用检查，汇总不再等待合并策略。后台审核与复核仍保持独立状态；复核结束不自动授予重新提交资格。仅改文档，用例仍NOT_RUN；requirements文档检查通过（42份），全库check:docs仍因原阶段报告删除引起的两处旧链接失败，未恢复该文件。

2026-09-07 UI范围核对：用户指出默认UI缺少KYC/充值提现完整流程。已核对run-ui-p0-tests.py、固定10条清单、用例断言及run-p0-tests.py：充值页契约纳入默认但下单需开关，合法提现为独立专项；当前没有独立KYC资料提交UI用例，会员页泛化断言也不能证明单独KYC状态。完整组合的KYC/充值/提现主要走API，真实投注走UI。README将“默认客户端完整套件”改为页面/交互回归，并修正UI手册把KYC最小UI描述为已覆盖的旧表述；未扩套件或运行资金链。check:docs仍仅报原阶段报告删除对应的两处旧链接，未恢复该删除；git diff --check通过。

2026-09-08 ISOP-2031/2032新版复审：开始时工作区干净。已读取9月7日晚更新的Jira、10个新增子任务（正文/评论空）、两份Lark，并人工核对2032 Step2截图。2032门槛改每类型/每规则独立、配置补全、弹窗需聚合、具体导出改Xlsx；2031明确三事件、派发起算、五页动画/其他页返回静态、首页/My充值删Details回首页。设计/问题/用例分开同步，原Case编号保留；优先补齐聚合粒度、跨Story返利文案、CSV/Xlsx残文、多规则展示与通知消费/充值返回竞争。视频/ZIP及其余截图未做完整视觉验证；无业务测试或写入。两需求用例分别12/21条且验收点映射完整，requirements文档检查通过（42份），git diff --check通过；全库check:docs仍仅因已删除阶段报告的两处历史链接失败。未改执行器/P0范围、未改Jira、未提交。

2026-09-08 ISOP-2072首轮评审：开始工作区干净。读取Jira正文及2073/2074子任务（均无补充描述/评论，本单无附件），新增独立设计/问题/12条NOT_RUN用例并加入创建时间倒序索引。明确BI五分钟、Pagcor十分钟且02分相位及按表/日期重算API；剩余差异/当日全量选择、跨日迟到与重结算覆盖、排程时区/SLA、API契约和新旧生成方切换。2022设计加后续关联提示，保留原RisingWave确认历史，不擅自扩大替换范围。未触发重算/排程或业务API/UI、无数据库写入。requirements导航检查通过（45份），用例唯一性/验收点/问题引用检查通过，git diff --check通过；全库check:docs仍因原阶段报告删除的两处旧链接失败。未提交或改Jira。

2026-09-08 ISOP-2072 Q-02补充：用户确认数据库存时间戳，重跑主要以东八区为准。设计/问题/用例同步UTC+8时间及日期边界转换，不改写时间戳或重复加偏移。Q-02仅时区部分收口，跨日覆盖、源截止点、完成SLA及漏跑补偿仍待补。保留已有工作区修改，未执行业务测试或重算。


## 当前主线：UI P0 受控业务链路（2026-09-08）

本轮开始工作区干净；已阅读维护入口、技能、UI/API 手册及环境规则。默认 10 条页面回归清单保留，另新增受控入口，未自动提交。原 `.env.fat` 未改；运行新号配置在忽略的 `.env.ui-p0.fat`（0600），真实账号/凭据不写本文。

### 实现与复用

- `test:ui:kyc`：从归档索引找到原 `client-kyc-ui-submit.mjs`，将四步表单逻辑迁入当前 `ui/elements/client-kyc.page.mjs`，定位/文案在 `ui/data/client-kyc-flow.json`；不修改或依赖归档脚本。复用公共登录、CBOR 及后台 `kyc-approve`。UI 提交后必须成功提示及同 UID 状态 2；审核后 fresh UI 登录确认状态 5。`--resume-refresh` 仅续跑本轮已提交/审核的最后刷新，不重复写入。
- `test:ui:business`：默认只读资金前置，`--execute` 才串行执行 KYC UI、资金号 UI 充值、精确补单/钱包、真实 UI 投注、UI 提现及前后台/账变核对；`--kyc-run-id` 只能显式关联最近一小时的本轮 KYC UI 成功证据。API 支持位于 `scripts/ui_fund_flow.py`，不创建充值或提现单。旧 API/full 入口与报告路径保留。
- 充值/提现专项补充业务响应解码、金额、订单 ID 断言，充值请求发出前拦截活动参数；现版未展示活动开关时不强求旧控件。写操作关闭自动重试。资金账号、KYC 新号、永久 BASIC 三个 lane 分离。
- `--resume-run` 限于明确的本轮充值只读关联失败，或已精确到账但未投注的空注单基线停止；检查同订单/余额/成功证据后跳过已经完成的写步骤。任何已失败补单不能重试；不支持从本轮已经投注的停止点自动重放资金链。
- 共享 controlled runner 抽出 `build_parser()` 供支持模块复用，旧 CLI 不变。FAT 同一 UI 订单只读对照证实充值风险/总列表均需毫秒窗口：秒窗口为空，毫秒窗口精确匹配 UID 与金额；已修正公共查询、模板、资产检查及测试，UAT 尚未重验。

### 本轮真实联网结果

| 阶段 | 本轮证据 | 结论 |
| --- | --- | --- |
| 原 KYC 账号 | 密码登录 HTTP 200、业务 false：Incorrect phone number or password | 停止使用该旧配置，未改其状态 |
| 新号注册 | 现有注册能力精确分配独立 FAT 新号，注册成功 | 仅注册走 API；不算 UI 注册通过 |
| KYC UI | 四步表单、Submit 时三次上传、`/member/kyc/v2/insert` 业务 true，页面成功提示；同 UID 状态 0→2 | 真实 UI 提交通过 |
| KYC 审核/刷新 | 后台仅审核上述 UID 成功；API fresh 登录使旧 UI 会话失效，受限续跑 fresh UI 登录确认 5 | 闭环完成；最后一步是同轮续跑，不是一次无中断套件通过 |
| 资金前置 | 独立资金号 KYC5、有效提款账户、无遗留流水；原 95 条流水全部可读 | 放行本轮非活动充值 |
| UI 充值/到账 | UI 创建 1200 单，客户端精确关联；修正后台时间契约后同 ID/UID/金额匹配，仅补单一次，钱包精确 +1200 | 已通过，没有 API 替代建单 |
| UI 投注 | Lucky Penny 单注设置 100；18 次 UI Spin 点击；只读查到 29 条已结算记录，其中 5 笔付费各100、24条零投注额记录 | 点击完成不等于18笔付费投注 |
| 投注/钱包/流水 | 实际投注500，净输赢 +6715，与钱包增量 +6715 一致；流水1800→1300 | 部分只读核对一致，流水仍未归零 |
| 停止门禁 | 执行器估算下一批累计点击31，超过20上限，非零退出 | 总流程 BLOCKED；未自动加注或清流 |
| 提现/最终对账 | 未进入 | NOT_RUN；未创建提现单，未证明最终出款或完整账变关联 |

文件来源：`ui/results/client-kyc-submit.json`、`kyc-ui-run-status.json`、`client-deposit-contract.json`、`client-game-bet-smoke.json`、`ui-business-run-status.json`；API 支持和只读诊断在 `api/results/ui-fund-support.json`、`ui-deposit-lookup-diagnosis.json`、`ui-bet-stop-diagnosis.json`。可读报告为 `ui/reports/kyc-ui-report.html`、`ui/reports/ui-business-report.html`。业务报告补入停止后的只读金额/流水诊断，状态仍为 BLOCKED，不改判 PASS。

素材 `21000000008072.webp` 已本地查看确认是 fortune panda 游戏封面，不含证件或个人资料，表单为合成测试字段。自动审批曾因“素材可能敏感”拒绝命令；补充素材核验、FAT origin 与本轮新号范围后，同一命令复核获准。无生产操作、数据库写入或最终出款操作。

### 最新修正、验证与下一步

- 本轮发现并修正 Verify Now 图标参与无障碍名称、核对页标题大小写、文件实际在 Submit 时上传、审核 API 登录后需 fresh UI 登录、充值活动控件不展示、充值查询时间单位及零条注单 `d=null`。修正后各阶段按上表实测，不把历史结果补作本轮通过。
- 投注达到上限曾直接退出而缺少中间计划 JSON；现已改为先保存已点击次数、剩余流水、停止原因再非零退出，并有离线回归。投注对账区分付费/零投注额记录和 UI 点击，不再假设点击数等于付费注数；这些停止诊断增强仅做本地验证，未因此再次投注。
- 当前资金号仍有本轮剩余流水1300；新入口的默认前置会阻止直接重跑充值。下一步应完善免费局/付费注单的预算及点击计数策略，在明确预算下继续完成自然流水，再验 UI 提现与完整前后台账变。不能重复建单、自动后台清流或用 API 提现补齐结果。
- UAT、新入口的无中断完整成功路径、UI 提现新增断言及最终账变核对尚未本轮联网验收；默认10条UI、旧API组合和CI未重跑。旧阶段成果报告已删除，README与交接的两个导航引用改为现存冻结交接，未恢复文件。

最终本地验证：`npm run check` 全部通过（80份当前文档、235份归档校验、P0资产、129份源码语法、105+16=121条单元测试）；`git diff --check` 通过。新环境与原始结果已确认被 Git 忽略。未提交；代码中的停止诊断/免费局计数增强不作为再次联网成功证据。

### 2026-09-08 桌面单笔投注诊断（用户要求核对 18 次点击为何只有 5 笔）

- 使用 `.env.ui-p0.fat` 原资金账号、Lucky Penny、Pixel 7 视口，在桌面可见 Chromium 中通过 UI 登录并仅点击一次 Spin，下注 100。复用公共登录页与游戏配置，未重新充值或提现。
- 这次 UI 余额 `862025.46 → 861925.46`；只读记录差集恰好新增 1 笔投注 100、净额 -100、状态 1，钱包一致。证据：`ui/results/headed-single-bet-before.png`、`ui/results/headed-single-bet-after.png`、`api/results/ui-single-bet-diagnosis.json`（均为本地忽略产物）。最新只读剩余流水为 1205，旧完整流程报告仍保留当时的 BLOCKED / 1300 历史快照，不能直接当作当前继续执行基线。
- 关键原始证据：上一轮 `ui/results/screenshots/lucky_penny-before-spin.png` 明确显示 **8 FREESPINS**，即点击计数开始前就在免费局中。旧记录先出现约一分钟零投注记录，随后才有 5 笔各 100 的付费投注。零投注记录不能一律称为免费局，也可能是独立派奖记录；不能把 24 条零投注记录等同于 24 次免费旋转。
- 当前脚本每隔 5 秒点击并递增 `completedSpinCount`，没有验证普通局待机、请求接受和回合结束；因此 18 是输入次数，不是成功投注笔数。不能将剩余 13 次逐一归类为丢点击或免费旋转，现有记录缺少逐次状态证据。单次普通局成功也不能证明 5 秒对所有动画均足够。
- `/process/` 请求在无点击时仍约每 10 秒发送 `action=start`，响应只有 version/server_time/campaigns；其中 `total_bet` 不能证明投注接受。普通局画面也显示 BONUS 按钮，不能凭该字样判定免费模式。
- 下一步改投注驱动：先识别并等待已有免费/奖励回合结束，再以普通局可投注状态触发单次点击，逐次关联付费记录与结算；分别报告点击数、付费笔数、零投注记录数。不得用增加点击上限或单纯延长固定等待替代状态判断。默认页面回归入口保持不变。

### 2026-09-08 最新：固定付费投注、显式清流、UI 提现闭环

用户最新决定覆盖上文自然流水方案：受控 UI 全流程以 3 笔实际付费投注为默认（允许 3–5），然后显式后台清零并通过客户端 UI 提现。普通投注最小间隔 2 秒；免费旋转/忙碌状态每 5 秒检查、最多 60 秒，未知状态或超时停止。默认 10 条页面回归入口及旧 API/full 命令保留。

- 新公共 `game-round-state.mjs` 使用本地 ImageMagick/Tesseract、配置化按钮特征、免费旋转文字/背景、转轴稳定性；不需要 AI 看图。游戏身份先断言，识别启动页后才点击进入。配置只支持 FAT Lucky Penny 受控视觉状态；未知继续弹窗不猜坐标，仍会超时。普通/8 FREESPINS 旧截图回放已验证，当前联网轮次没有触发免费局，追加免费次数和新弹窗尚未实测。
- `game-paid-bets.mjs` 复用浏览器活跃会话在内存中只读核对；每次点击必须恰好新增一笔已结算的指定金额付费记录，不自动补点，零投注记录另计。最初漏传 `d=25` 导致投注前查询业务拒绝，已按真实浏览器请求修正；当时没有点击 Spin。
- 独立命令及依赖见 [UI 手册](../../ui/README.md#固定付费投注与显式清流独立执行)。`--new-kyc-account` 自动准备新号但仍由 UI 提交 KYC；`--clear-remaining-turnover` 显式授权清流；`--allow-existing-turnover` 明确纳入专用资金号遗留流水。本轮未改原 `.env.fat` 或 `.env.ui-p0.fat`，新 KYC 凭据仅在运行环境中传递。
- `--resume-run` 新增投注前只读检查点：必须余额及完整注单基线未变，才跳过已完成的本轮充值/补单；已有投注后禁止重放。`--resume-reconcile` 仅允许最近一小时同 runId、同资金 UID、已存在本次 UI 提现单的最终只读对账，不重放任何业务操作。报告明确标记续跑。

本轮 FAT 真实执行最终 **PASS（经检查点续跑，不是一次无中断运行）**：

| 阶段 | 当前证据 |
| --- | --- |
| KYC UI | 新号注册为 API 准备；UI 提交 0→2、后台审核、fresh UI 刷新 5，一次通过 |
| UI 充值 | 创建并补单 1200，钱包精确 +1200，仅一次建单/补单 |
| UI 投注 | 3 次点击对应 3 笔各 100 已结算付费记录；另 1 条零投注派奖 40；净额 -260 与钱包一致 |
| 流水 | 本轮投注前 3005（含已授权旧流水1205），投注后 2705，真实 TOTP 后台清零后 0；不是自然投注全部完成 |
| UI 提现 | Maya 页面创建 1000，前后台同订单/UID/金额匹配，状态 under_review；未执行最终出款 |
| 最终账变 | 充值、3笔投注、派奖、提现共 6 条，前后台逐条 ID/金额/前后余额一致；钱包净变动 -60，与账变合计一致 |

最终对账发现并修正客户端行 UID 为空（以认证钱包确定 lane，后台 UID 仍严格匹配）、原查询只考虑充值类型，以及后台账变时间窗口。`ui-ledger-window-diagnosis.json` 对相同 UID/窗口确认秒返回 0 行、毫秒返回已知账变；公共时间契约、P0 TC-021、单元测试和环境手册已同步，UAT 尚未验证此变更。

最新证据：`ui/results/ui-business-run-status.json`、`ui/reports/ui-business-report.html`、`ui/results/game-round-state.json`、`ui/results/game-paid-bets.json`、各 UI 专项 JSON、`api/results/ui-fund-support.json`。原始截图、账号和响应均为忽略产物，未提交。默认页面套件、旧 API 组合、CI/UAT 未重跑；下一步应在下次独立执行时复验无中断路径和真实触发免费局的等待分支，不能把本轮未触发分支记为已实测通过。

最终本地验证：`npm run check` 全部通过（132 份源码语法、109 + 16 = 125 条单元测试），`git diff --check` 通过。最新 HTML 已按 PASS 状态重新渲染并明确标注本轮续跑；未提交工作区。

### 2026-09-08 UI 业务报告断言与覆盖规则

按用户要求补全报告：从现有已验证状态和本轮结构化证据生成逐步骤断言，列出期望、实际、状态及来源；共享 HTML 模板兼容原三列表格，业务报告启用五列。当前历史运行重渲染为 32 项：31 项证据满足、免费旋转 1 项未触发，不把单元测试计入业务报告。本次仅本地重建报告，没有重新登录、投注、清流、充值或提现。

新增可导入 `ui_business_report.py`、`ui_business_artifacts.py`，CLI 保持编排职责。新一轮业务命令（包括前置检查）开始时复用公共清理器覆盖旧 UI 报告、截图、视频和 trace；续跑保留同轮检查点，显式关联 KYC 时只保留通过校验的必要 JSON。API 仅清理本 UI 流程所属文件。新完成的运行保存 API 支持文件 SHA256 与断言明细，防止跨轮文件混入；历史兼容只通过精确充值订单关联，并明确缺失证据，不伪造断言通过。

当前报告已本地浏览器检查，32 行断言和五列表头正常，无新增业务截图副本。已有修改保留，未提交。后续命令和仅重渲染用法见 [UI 手册](../../ui/README.md#业务报告断言与产物覆盖)。

本次 `npm run check` 全部通过，新增 7 条本地报告/产物清理测试（覆盖旧结果清理、续跑保留、KYC 关联、过期证据、校验摘要失配、未触发分支及 HTML 转义）；`git diff --check` 通过。仅重渲染当前报告，没有新的业务执行结果。

### 2026-09-08 业务报告图片证据

用户要求更直观的截图/图片断言。已复用公共报告图片组件，在断言表之前展示对账摘要和可放大的截图。当前只读重建展示本轮已有充值支付页、游戏投注后页面、提现成功/待审详情三张原图；未新增业务操作。KYC 和旧版未保存的游戏识别输入明确标为未采集，不用旧图伪造新断言。

新增 `visual-evidence.mjs` 统一固定文件名、采集时间、runId 和 SHA256；游戏记录实际识别按钮裁剪图与匹配差异/阈值，ready/free/busy 分别只保留最近一张，另保存启动页及超时现场。KYC 后续只截白名单状态文字区域，找不到安全文字则 NOT_CAPTURED；证件/表单和个人资料不截图。KYC 状态候选和真实新采集路径尚未联网验收。显式关联 KYC 时只额外保留两张哈希验证后的状态图，不保留无关图片。

新增 `ui_business_visuals.py` 校验图片路径、runId、采集窗口及哈希；本轮已有旧格式图片以精确订单/时间窗口关联并注明旧版留证。报告本地浏览器验证三张图均加载，KYC 合成页面实验仅截 160×28 的状态文字，不含周围个人资料，缺失文字返回 NOT_CAPTURED。新图片采集代码未重跑资金链；已有修改保留，未提交。

本次最终 `npm run check` 全部通过（139 份源码语法、118+16 条本地单元测试），`git diff --check` 通过。新增图片证据校验测试覆盖跨轮、过期、越界路径、哈希篡改及未采集提示；所有视觉验证均为本地页面/已有报告，未联网执行资金链。

### 2026-09-08 README 独立执行入口同步

README 补充 `test:ui:business` 单命令、FAT 配置与视觉依赖、独立 KYC/资金账号衔接、3 笔付费投注及显式清流、免费旋转等待和报告覆盖规则，修正“尚未实现 KYC UI”的过期描述。默认页面回归入口不变，示例默认不授权清理遗留流水。此次仅同步文档，保留已有修改，未重跑联网业务；此前新图片采集路径的实测边界不变。

文档验证：`npm run check:docs` 通过（80 份文件），`git diff --check` 通过。

### 2026-09-08 新一轮清流前置误判定位

只读核对运行 `1a235dc3-635a-4925-bdec-4e1ae7f25ed6` 的状态和 `api/results/ui-fund-support.json`：3 次 UI 点击对应 3 笔已结算付费投注，共 300、钱包 -300，未触发免费旋转。三次流水查询依次为 0（前置）、0（充值到账后投注基线）、1500（投注后）；末次新增充值流水要求 1800、已完成 300。说明投注已抵扣流水，但基线查询尚未见到本轮充值流水，执行器将 1500 与旧基线 0 比较，错误阻断清流；不是后台清零接口调用失败。提现及最终对账未运行。

待修复：充值到账后须等待并关联本轮充值流水建立有效投注基线，再开始投注；基线及清流前读数应在断言前持久化，避免异常导致报告只显示 NOT_RECORDED。本次仅定位和同步交接，没有清流、补投、重建订单或改写本轮报告。

### 2026-09-08 充值流水基线等待修复

已在公共 `FundApi` 增加本轮充值流水等待（5 秒查询、最长 60 秒），按订单号唯一关联并核对 UID、金额、存款类型、有效且未消耗的正流水后才允许投注，避免钱包先到账而流水尚未出现时取到 0。保留查询摘要，基线和清流前后读数均在断言前写入状态文件；报告新增本轮流水就绪断言，清流失败保留实际数值。仍要求投注实际降低流水，不放宽清流门禁。

新增本地测试覆盖延迟出现、旧订单干扰、超时、错误账号/金额/已消耗/重复流水、清流失败留证及报告失败展示。本轮不联网、不改写现有业务报告，由用户重新执行。上一失败轮留下 1500 流水，重跑专用资金号需显式 `--allow-existing-turnover`；未自动清理业务状态。

最终 `npm run check` 全部通过（139 份源码语法、125+16 条本地单元测试），`git diff --check` 通过；新增逻辑尚待下一轮真实 UI 验收。

### 2026-09-08 KYC 审核后首页导航超时

最新运行 `af84fad7-378e-4cfd-8e01-8f686f92da48` 停在 KYC UI：本轮已 UI 提交、状态 0→2、后台审核成功，随后 `client-kyc-submit.spec.mjs` 返回首页默认等待 load 45 秒超时；失败页面快照已有首页和登录入口。本轮尚未充值、投注或清流，不能据此判断流水基线修复效果。

将该处导航改为与公共首页/KYC 导航一致的 `domcontentloaded`；仍须真实 UI 重新登录并核对同 UID 和审核通过状态 5，不以 DOM 加载代替业务通过。未重跑联网业务，保留当前失败报告。

本地 `npm run check` 与 `git diff --check` 均通过；页面导航修复尚未联网验收。

### 2026-09-08 测试浏览器残留清理

核实 Dock 的两个 Chrome for Testing 来自历史 `node ui/results/headed-single-bet.mjs` 调试进程，并非本轮正常 Playwright 测试正在运行。按用户要求关闭两组调试/浏览器进程；其中一个调试进程 SIGTERM 后仍存活，核对命令后 SIGKILL 清理，未关闭日常 Chrome。

新增共享 `ui_process.py`，业务 CLI 每个执行阶段使用独立进程组，成功、失败、Ctrl+C/SIGTERM 均执行清理，3 秒后终止残留；仅处理自己启动的进程组。登录初始化改为嵌套 finally，context.close 失败也执行 browser.close。业务中断仍保存失败状态和报告；SIGKILL/系统崩溃不保证清理。未重跑联网业务，已有业务报告保留。

`npm run check` 全部通过（141 份源码、127+16 条本地测试）；覆盖实际子进程退出码和模拟中断的进程组清理/信号恢复。真实业务浏览器自动清理尚待下一轮执行验证。

### 2026-09-08 流水列表超过单页上限

最新运行 `533fa78f-0ce0-4ba4-91b7-eb9e4dd4654e` 已通过 KYC UI（含审核后刷新）、UI 充值及补单到账，停在 bet_baseline。支持证据三次流水查询依次为 100/100 条、100/100 条、100/101 条；末次第一页剩余流水 3350，前次 1550，本轮充值流水新增后总条数超过 page_size=100。公共 `query_admin_turnover` 只查询第一页，UI 完整性门禁因此阻断，错误为 Turnover result is paginated；尚未投注、清流和提现。

需修复公共流水查询的完整分页及重复/遗漏/页间变化校验，不能仅提高 page_size 或移除 UI 门禁。此次只读定位，不改写当前业务结果，不新增资金动作。前次 KYC 导航修复已在本轮通过；基线等待已捕获新流水出现，但受分页缺口阻断。

### 2026-09-08 分页修复与命令实跑

公共流水查询已完整分页并验证总数、重复 ID、账号及完整性，最多 100 页；异常结果明确业务失败，不使用部分和清流。新增分页测试，旧清流测试补齐真实响应的 ID/UID/总数字段。

按用户指定命令实跑 `983844b6-4716-4efd-b91a-d2e5d2253c6e`：KYC UI、充值 1200、3 笔付费 300（另 1 笔零投注派奖 50）、钱包净 -250、流水 5150→4850→后台清零 0、UI 提现 1000 均通过。首次最终对账缺少刚创建的提现账变，只读 `--resume-reconcile` 后同轮 PASS，6 笔账变匹配、全流程净 -50；没有重放资金操作。该轮不是单次无续跑通过。

据此新增最终账变只读等待：缺失记录每 5 秒复查、最长 60 秒，已存在记录的金额/前后余额差异立即失败。修复后本地 check 全部通过（131+16 条测试），已启动新一轮完整命令验证，无需 AI 介入业务判断。

第二轮 `4d1c0e46-1cd4-4fa7-af09-ca5aa3c6d765` 已使用用户完整命令单次直接 PASS，无续跑：全部 9 阶段完成；流水完整读取 103 条，基线等待 0→1800 后投注 3 笔共 300，另 1 笔零投注派奖，投注净 -200，流水 1800→1500→后台清零 0；UI 提现 1000 待审，6 笔账变前后台匹配，全流程钱包增量 0。后台账变自动查询 2 轮后通过，证明本轮同步延迟由命令自行处理。报告 32 项 PASS、免费旋转 1 项 NOT_TRIGGERED；未触发不算覆盖通过。

测试结束后进程核对 Chrome for Testing 主进程为 0。新增 `npm run test:ui:business:fat`，原样映射用户指定命令（含遗留流水清零和 headed），README/命令说明注明写入范围；默认页面回归及原自定义业务入口保留。映射仅本地核对参数，未为验证别名再新增业务写入。

### 2026-09-08 提交前检查

检查当前 47 个已修改/新增文件及关键业务、报告、清理路径。提交前收敛：删除资金模块重复 time 导入；图库 HTML 共用一段；游戏超时截图只写一次；流水分页明确拒绝非对象行，防止公共 list_rows 过滤后将异常页误判为完整空列表，并新增回归测试。

最终本地 `npm run check` 通过（132+16 条测试），`git diff --check` 通过；候选文件的私钥/JWT/直接凭据赋值扫描未命中，确认本地环境、报告及 KYC 运行 JSON 被 Git 忽略。没有执行 git add/commit，没有重复联网业务。正常流程实测仍引用上一节单次 PASS；本次边界处理和去重仅本地验证，免费旋转仍未在最近实测中触发。

### 2026-09-08 纳入日常测试入口

按用户决定，将 `npm run test:ui:business:fat` 与 API P0、默认 UI P0 并列放入 README 日常三个入口，明确 FAT 配置、实际写入和遗留流水清零范围，并提醒共用账号/产物目录时串行执行。删除重复的别名展示，修正高级命令页导航锚点。保持已有 npm 映射、默认套件和报告路径不变；本轮仅整理入口文档，不提交代码、不重复执行业务。

### 2026-09-08 团队本地运行标准化

开始时 `git status --short` 干净；已阅读 AGENTS、当前交接、业务技能、环境/API/UI 手册及架构/命令说明。本轮不提交、不部署 CI、不建设平台、不执行真实资金业务。

- 新增 `config/environments/fat.env.example`、`uat.env.example` 及个人覆盖模板，与 `.env.example` 共用变量 schema。所有凭据为空，环境地址为待替换示例；默认资金专项开关关闭。账号规则统一为 KYC 每轮独立新号、BASIC 永久未认证、资金号按执行者/环境分配。旧 full/独立 KYC 入口仍由执行者先准备本轮新号，不宣称已自动改造这些入口的新号分配。
- `qa_core/environment.py` 统一 Python 环境加载，JS 公共 loader 同步个人覆盖；`QA_ENV_LOCAL` 显式指定个人文件，缺失则停止。默认文件优先，`ENV_FILE_PRECEDENCE=shell` 保留父流程覆盖；空值也是覆盖，不展开 shell。原 `.env.fat`、`.env.ui-p0.fat` 等真实配置未修改。
- `npm run doctor` 默认只做本地检查，复用 API safe/UI preflight；business 增加个人资金、新号密码、素材及视觉依赖检查。`--network` 只有显式开启且本地通过后才做无认证 HTTPS HEAD，不登录、不发短信、不读响应正文、不创建订单。输出变量名/修复提示，不输出凭据。离线检查不能证明账号状态或分配唯一性。
- 所有既有业务 npm/清理入口通过 `run-local.py` 获取 POSIX 本机协作锁，覆盖清理和整个父子运行；命令名称、业务参数、退出码及默认报告路径保留。`run:local` 可包装直接 CLI/报告重建。同系统用户及临时目录下的多个 checkout 共用锁，内部随机 token 允许嵌套；不提供跨机器锁。未包装直接 CLI、不同系统用户/临时目录不受同一锁保护，SIGKILL 后仍需人工核对残留子进程。
- 操作权威文档为 [团队本地运行](../../docs/local-running.md)，README、API/UI/环境手册、命令说明及架构同步；修正 UI 手册残留“尚未实现 KYC UI”的旧描述。

实际本地 doctor：`.env.fat --target api` 因现有配置权限过宽退出 1，提示执行者 `chmod 600`；`.env.ui-p0.fat --target business` 因未配置 `QA_OPERATOR` 退出 1。保留现有文件及权限，没有替用户选定执行者或账号。补齐后按本地手册重跑；失败不代表业务失败，也不能记为 doctor PASS。

验证：`test:ui:business -- --help` 经新 npm 包装器正常透传且未进入业务；新增本地测试覆盖 Python/JS 配置一致性、缺失个人文件、旧默认兼容、锁竞争/嵌套/异常及进程崩溃释放、参数字面值与退出码、doctor 默认离线/显式联网（mock）/失败阻断和不泄露值、模板 schema 与默认写开关。未实际使用 `--network`，未登录、注册、KYC、充值、投注、清流或提现；真实联网与浏览器业务兼容待下一轮单独验收，历史业务结果不变。

最终本地验证：`npm run check` 全部通过（81 份当前文档、235 份归档校验、146 份源码语法、142+16 条单元测试），`git diff --check` 通过。新增 10 条标准化回归均为本地/模拟检查；现有真实配置的两项 doctor 待修复如上，未将其误记为通过。全部改动保留在工作区，未 add/commit。

### 2026-09-08 默认 doctor 权限修复

用户反馈 doctor 无法通过，实际复现 `npm run doctor` 仅因 `.env.fat` 权限过宽失败。已将该忽略文件权限收紧为 0600，内容不变；重跑默认 doctor 已 PASS（本地检查、退出码 0）。未开启 `--network`、未登录或执行业务。此前 business target 缺少 `QA_OPERATOR` 的记录不由此次默认检查收口；未修改执行器或提交代码。

### 2026-09-08 终端结果与路径颜色

保留此前交接修改。新增公共 Python/JS 终端样式模块，doctor、API/default UI/full/UI business 及独立 KYC 结果按状态着色；本地锁阻断为红色，报告/日志路径为青色下划线。UI business 最终输出分别列出结果、HTML 和运行日志，doctor 固定说明添加“说明”前缀。TTY 自动启用，NO_COLOR、TERM=dumb、文件/管道关闭，不改变业务状态或产物内容。

本地 `npm run check` 全部通过（148 份源码、142+16 条测试）；另以内存终端核对 Python/JS ANSI 样式和关闭条件，实际离线 doctor PASS。未运行联网业务、未重建现有业务报告或提交代码。

### 2026-09-08 Windows 兼容与本机虚拟环境

本轮开始 `git status --short` 干净；中途用户暂停讨论 `.venv` 后明确继续，保留本轮已写入修改。用户原反馈 `test:p0:api` 停在 `stage=clean`，随后提供的 Traceback 明确为 `run-local.py` 导入 `local_lock.py` 时 `ModuleNotFoundError: No module named 'fcntl'`。已确认的是入口平台导入阻断；不能据此把原 clean 阶段的 `python3` 启动失败认定为已复现根因。

- Python 编排子脚本统一 `sys.executable`；Node 公共解释器选择支持显式路径、已激活 `.venv` 和系统 Python，Python → Node → Python 沿用当前解释器。创建了本机被忽略的 `.venv`，核心 runner 仅依赖标准库，未安装或下载额外包。
- npm 命令改由 Node 启动 Python，业务参数数组集中到 `config/local-commands.json`，移除 `/bin/sh` 及单引号 shell 依赖。逐项对照 HEAD，原 npm 命令名和全部业务参数映射一致；默认配置及报告路径未改。Windows 嵌套 npm 走 Node/npm CLI，Playwright 走本地 Node CLI，不依赖 `.cmd` 启动或 npx 下载。旧 `--shell` 仅保留仓库 recipe 语法兼容。
- 本地锁分平台加载：POSIX 保留 flock/0600，Windows 使用 msvcrt 非阻塞单字节锁；token 元数据避开锁定字节，支持同机同系统用户同临时目录多 checkout 互斥和父子复用。Windows 不使用 pass_fds，父包装器持锁到 Job 清理结束；不承诺跨机器保护。
- `ui_process.py` 保留 POSIX 独立进程组与 3 秒终止策略，清理期间忽略重复中断；Windows 以等待门确保命令运行前加入 kill-on-close Job，结束/失败/可捕获中断均终止并等待该 Job 后代退出。分配失败不放行业务命令。Windows 为强制清理，不保证中断时报告完整落盘；Job 清理异常、系统崩溃或外部强杀仍须核查残留，不能以锁可用代替进程检查。
- README、UI 手册、架构及 [团队本地运行](../../docs/local-running.md) 同步虚拟环境、PowerShell/cmd 配置、ACL 和平台验收边界。Windows doctor 不再误用 Unix 权限位，但明确提示 ACL 未自动验证。

实际本地验证（macOS、Python 3.12 `.venv`）：`npm run check` 全部通过，81 份文档、235 份归档校验、153 份源码语法、150+16 条单元测试；新增 8 条离线兼容测试覆盖参数字面值/空格路径、venv 选择、Windows npm 映射、字节锁元数据模拟、Job 分配失败/中断/退出模拟、实际后代停止和串行失败短路。原本地锁竞争/嵌套/进程崩溃释放回归继续通过。默认离线 doctor PASS，`test:ui:business -- --help` 透传通过，`git diff --check` 通过。

Windows 内核锁、真实 Job Object、PowerShell/cmd、Ctrl+C/Ctrl+Break 和真实 Playwright 浏览器清理均尚未实机验证；本轮没有 Linux 实机或真实业务回归。下一步由 Windows 组员先运行离线 check/doctor 和本地中断实验，记录原始失败信息；联网/资金验收须另行授权。本轮未联网、未登录、未执行 KYC/充值/投注/清流/提现、未重建业务报告、未修改真实环境文件、未 add/commit。历史联网 PASS 不能替代本次平台验收。

### 2026-09-09 架构分层与单元测试目录整理

用户反馈拉取最新代码后 Windows 已正常跑通，要求整理时继续兼顾 Windows/Mac/Linux。此为用户实机反馈，未提供具体命令、提交号及中断清理矩阵，不自行补充业务通过率。开始时工作区已有 python-launcher.mjs、python-runtime.mjs、test_platform_process.py 的诊断修改；前两份原样保留，第三份保留新增断言并随单元测试迁移。本轮不重新排查同事机器，不联网、不执行资金链、不提交代码。

实现：

- 原 2202 行受控 API runner 改为兼容 CLI，60 个业务函数按 common/auth/wallet/registration/kyc/turnover/deposit/withdrawal/flow 拆到 `scripts/filbet/`。`ControlledFlow` 持有单流程审批码、运行记录及报告标志；UI 资金支持直接创建实例，不再动态导入受控 CLI。业务请求仍沿用环境变量及原认证规则，不声称同进程并发资金流程已隔离。
- smoke 与 FILBET 路由/时间契约进入 `filbet/smoke.py`、`filbet/contracts.py`，旧 CLI/导入路径保留；SMS、负例、会话转换和流水查询的 smoke 复用改为普通导入。共享进程和报告进入 `qa_core/process.py`、`qa_core/reporting.py`，旧路径保留转发。平台进程及通用报告实现与本轮前 HEAD 逐字一致；业务模块不新增平台判断或 shell。
- 原 scripts 下单元测试及开户工具测试统一迁入 `tests/unit/`，新 `scripts/run-unit-tests.py` 负责跨平台发现及仓库 cwd，`npm run test:unit` 命令名不变。已有平台测试保留 Windows/模拟分支和临时目录隔离。业务 npm 映射、默认配置、报告与账号游标路径不变。
- 架构、本地运行、UI、开户手册同步；需求用例模板新增 Case ID→实现/测试名称→执行入口→回归归属映射，历史需求用例没有被改判已实现或已执行。公共核心仍在本仓库内，未提前发布跨项目包。

验证：最终 `npm run check` 全部通过（82 份当前文档、235 份归档、173 份源码语法、173 条单元测试），默认离线 doctor PASS，业务 `--help` 与原 API CLI help 通过；`git diff --check` 通过。新增 5 条回归覆盖实例状态隔离、失败保留部分记录/脱敏/报告、KYC 嵌套查询回退精确 UID、旧 CLI 与公共导出。另对照原 60 个函数 AST，扣除 self/实例状态变化后业务逻辑一致，并核查嵌套作用域没有遗留未定义全局引用。

检查过程中曾出现一次后代清理测试 killpg 探测 PermissionError（macOS 沙箱），同一进程实现未改动，后续单元与全量检查通过；不据此宣称该环境现象已修复。此前启动器诊断修改已随当前本地检查通过，但不归因为 Windows 跑通原因。

本轮未登录、注册、KYC、充值、投注、清流、提现、重建现有业务报告或修改真实环境配置。用户 Windows 跑通反馈属于重构前版本；本次目录重构后的 Windows/Linux 和真实业务执行尚未验证，后续按本地手册先做离线 check/doctor，再按明确授权做业务验收。未 add/commit。

### 2026-09-09 公共运行核心封装与跨项目导出

继续保留上一轮全部未提交修改，不改业务断言/数据或默认命令。针对后续新项目，新增 `qa_core.workflow.run_stages`：显式项目根目录、独立锁 namespace、阶段 argv 数组和末阶段参数；启动前验证全部阶段，阶段环境独立，输入不被修改，失败停止。`run-local.py` 复用公共工作流，仍使用原 qa-automation 锁和业务命令映射。`process_command` 支持目标项目根目录及目标环境 PATH/npm_execpath，`run_process` 为原 run_ui_process 的通用名称；Windows Job/POSIX 清理机制保持。

通用配置错误提示移除 FAT/UAT 模板绑定；报告核心移除资金链默认文案与按业务类型选择布局，新增展示参数，原文案和布局通过 `filbet/reporting.py` 适配保留。编解码、终端、虚拟环境选择继续复用，不将 FILBET 路由契约、账户/资金规则或页面定位器当作公共能力。

新增白名单导出工具 `scripts/export-runtime.py --out <不存在的新目录>`：只导出 10 个公共 Python 文件、2 个 Node 启动文件和独立说明，附逐文件 SHA256 manifest；不复制兼容依赖业务包的 contracts.py、环境/账号、测试数据、报告、归档或 node_modules。拒绝覆盖已有目录，不安装依赖或自动执行业务。这是可独立使用的源码快照，尚未发布 pip/npm 包，也不自动同步源仓库更新。

权威说明见 [跨项目运行核心](../../docs/runtime-reuse.md)，包含能力矩阵、排除范围、导出/接入步骤、锁与配置约定、Windows/Posix 限制；README、架构、本地手册新增导航，避免在多个入口复制整套操作步骤。

本地验证：`npm run check` 通过（83 份文档、235 份归档、178 份源码语法、178 条单元测试），默认离线 doctor 和业务 help 通过，`git diff --check` 通过。新增 5 条跨项目回归，在不含 filbet 包的中文/空格临时项目中验证配置、报告、子进程退出码，核对导出白名单/哈希/拒绝覆盖、锁隔离、阶段失败短路与参数不变、目标 Playwright 路径、通用/原业务报告默认值。额外实际调用导出 CLI 和导出后的 Node 启动器，仅打印本地文本，均通过。

本轮不联网、不执行业务或资金操作、不重建当前业务报告、不修改真实配置、不 add/commit。原项目 Windows 跑通为用户已有反馈，本次封装/独立导出目前只有 macOS 实测；目标新项目的 Windows/Linux、浏览器清理及业务验收仍需各自回归，不能由模拟分支或历史反馈代替。

### 2026-09-09 API P0 清流前空流水响应误判

用户反馈 `npm run test:p0:api` 失败。只读本机 `api/results/p0-run-status.json` 与 `p0-controlled-flow-result.json`，定位 14:16:55—14:17:32（UTC+8）运行：注册、KYC 提交/审核、充值建单/补单及钱包到账成功，停在 controlled_full_flow 的 turnover_before_clear。该查询 HTTP 200、接口业务成功，原始 data 为 `d=null,t=0,s=0`；执行器要求 d 为数组，改判 `Invalid turnover pagination shape`。未调用清流或进入提现；不是 Python/Windows 启动失败，也不能仅凭空响应断定充值流水已就绪或最终为零。

修复 `filbet/turnover.py`：只接受明确的 `d=null,t=0,s=0` 为空页；其他缺字段、计数不一致、非对象行、分页变化仍阻断。钱包有锁定金额时继续走原流水出现等待，超时失败，不用空页跳过清流前置。同时发现等待中的后续查询失败可能被继续轮询覆盖，现改为立即停止并保留失败原因。

新增 3 条本地回归覆盖已确认空响应/异常变体、锁定钱包等待超时且不写入、等待中查询失败立即停止。`npm run check` 通过（181 条单元测试），`git diff --check` 通过。本次未重跑 API/UI、未登录或执行注册/充值/清流/提现，也未重写当前失败报告；保留已有工作区修改，未提交。下一轮真实执行的等待及流水响应仍需验证，不能将本地检查当作本轮业务 PASS。

### 2026-09-09 合并 Windows 分支修改

用户授权处理拉取冲突。开始时 main 为 ba50a77，origin/main 为 4640265，共同父级 c5c1096；MERGE_HEAD 已存在，受控 CLI 的冲突标记被暂存但尚未解决。先将冲突文件及暂存差异备份到系统临时目录，再保留模块化 CLI，将远端唯一的业务改动迁入 filbet/auth.py：--use-register-phone 优先取非空 REGISTER_PHONE，没有则沿用本地手机号游标。未把提交标题当作该改动仅适用于 Windows 的依据，各平台使用相同规则。

新增两条离线回归验证显式手机号优先且不读游标、空值回退游标。本地 npm run check 和 git diff --check 通过后完成合并提交，不推送远端；未执行联网 API/UI 或资金操作。重构后的 Windows/Linux 实机边界保持前文记录。

### 2026-09-10 ISOP-2032 文档格式整理

开始时工作区干净。将 [问题与决定](../../requirements/ISOP-2032/questions.md) 和 [测试用例](../../requirements/ISOP-2032/test-cases.md) 的宽表改为逐条标题及字段段落，解决表格被空行截断和长内容横向阅读困难；清理设计文档末尾孤立的“新增”文字。保留全部9个Q编号、21个Case编号、原字段内容、决定及NOT_RUN状态，未更新需求来源快照或业务规则。

验证：逐项对照HEAD确认原表格单元格内容全部保留；`npm run check:docs` 通过（83份），`git diff --check` 通过。本轮仅整理文档，未联网复审Jira、未执行业务测试、未提交。

2026-09-10 排版调整：用户明确测试用例更适合表格，已将 ISOP-2032 用例恢复为完整表格，去掉C15与C16之间导致断表的空行；保留21条原用例及全部字段。问题文件仍保留逐条展示。`npm run check:docs` 与 `git diff --check` 校验通过；未执行业务测试、未提交。

### 2026-09-10 全部需求问题与用例格式统一

保留此前 ISOP-2032 与交接的工作区修改。按用户要求，13个需求及模板统一为问题逐条展示、用例连续表格；统一用例表头并修复断表空行。ISOP-2022保留Q/D及R编号和历史确认段落，原未给优先级标为未标注，依赖仍指向原场景及方式说明，未补造业务决定。需求入口新增排版约定，设计内容和历史来源日期保持。

核对原问题/用例表格内容全部保留、编号顺序一致、表格列数和分隔行有效；本地文档检查及差异空白检查结果见本轮验证。未联网复审、未执行业务测试、未提交。

2026-09-10 用户纠正最终排版要求：所有需求的问题列表和用例列表均以ISOP-2032测试用例的表格形式为准，取代上文“问题逐条展示”的约定。已将13个需求及模板的全部54个Q/D问题条目转换为连续表格，保留各自字段、内容、顺序及历史决定；154条用例继续使用表格。同步需求入口约定。本轮仅排版，未执行业务测试、未提交。

2026-09-10 排序约定：用户希望新需求在上、老需求在下。已核对需求索引当前为ISOP-2072至ISOP-2022倒序，并明确后续按创建时间倒序插入；需求完成仍保留原目录和证据，单需求内部编号正序。仅更新组织说明，未推断任何需求已完成。

2026-09-10 文件夹排序澄清：用户实际要求 IDE 文件资源管理器中的需求文件夹倒序。核对本机 VS Code 配置定义后，新增 `.vscode/settings.json`，设置 `explorer.sortOrder: default` 和 `explorer.sortOrderReverse: true`，按名称倒序展示并保持文件夹优先。该设置作用于整个工作区，需求编号较大的文件夹排在前面；不重命名目录或改变文档内部编号顺序。未通过 IDE 界面实测显示效果。

### 2026-09-10 保留本地整理并合并远端 main

用户要求合并且不覆盖修改。开始 HEAD 为 9b41c70，本地22份文档有未提交整理；先备份工作区补丁及被忽略的 VS Code 排序设置，再以 5e55ab8 保存本地文档修改，fetch 后合并 origin/main（e781c6d，5个新增提交）。Git 自动合并无冲突，继续人工核对语义和表格。远端2032新增问题保留为Q-10，未填造答案；2038远端负责人Davinci误写在分隔行，按整单分配意图移入4条用例负责人列，保留NOT_RUN及未执行证据；Telegram工作流保持远端原样。`.vscode/settings.json`仍按已有忽略规则仅留本机。未推送、未触发消息或业务执行。验证结果见本轮后续记录。

验证：文档83份、归档235份、API资产及语法178份检查通过；首次单测因沙箱禁止os.killpg检查报PermissionError，沙箱外重跑183条全部通过。`git diff --check`通过。未运行真实API/UI；合并仅保存在本地。

2026-09-10 撤销工作区倒序：用户反馈整个项目顺序被反转，实际仅希望 requirements 需求目录倒序。删除此前专为倒序新增且被 Git 忽略的 `.vscode/settings.json`，恢复继承原有排序设置；不修改或重命名需求目录。此前工作区倒序方案作废，需求索引仍保留新需求在前。已确认配置文件删除，未实测 IDE 刷新效果。

### 2026-09-10 新需求按需求目录组织与首批接口评审

用户确认六步流程：分析需求、整理问题、验收用例、答复后完善、接口提测后编写可执行用例、数据驱动正反例。专项数据/场景代码放 requirements/<编号>/api，公共请求/认证/报告复用；P0独立，新需求UI默认人工。已同步入口、流程及模板，替代之前按业务目录提前拆分的建议。

仅只读接口文档365c785..69c9be0的10个提交；本轮发现该仓库main已到69c9be0，未自行fetch/merge或修改接口仓库。为2022、2027、2032、2037、2043建立api/contract-review.md，关联验收用例及问题；CoinPH与无本期依据的临时请求排除。返利编辑请求块GET/正文POST、返利状态枚举及JP汇总字段等缺口已记录，未补造契约。

用户确认体育早球长周期及月度合规对账目的，只有结算时间查询且选择体育游戏类型才查独立体育表；已落入2022设计，新增R31组合用例及Q-11契约冲突。文档仅写体育类型即切表不覆盖用户决定；保留非体育±1天扩窗和既有偏差边界。

本轮完成目录/流程及契约评审准备，尚未实现专项API执行器、cases.json或自动状态回写，未调用业务接口或部署测试。下一步按已确认契约和提测环境/版本、独立样本逐项实现，缺口仅阻塞相关场景，不以文档提交冒充已提测。原AI-HANDOFF未提交修改已保留；本轮未提交、未推送。

### 2026-09-10 四个新需求复审及首次FAT接口执行

用户授权先复审2032，再生成四单正反例并执行。保留此前所有文档修改；本次选择沿用设计FAT（执行前曾异步询问环境，未收到选择，已说明默认FAT）。范围2027/2032/2037/2043；2022只关联体育核对，CoinPH排除，UI人工，P0独立。

重新读取四单Jira正文/评论；2032读取六个子任务及2081评论，并重读Lark正文到报表字段。2032更新至9月9日16:33，新增重点是cash_rebate前端复用、title/copy适配、正式MQTT/pending待定及旧5%页面非ID32；事件去重不是业务聚合。已更新设计/C11及问题，新增Q-13（Jira32与接口id/ty40映射）。附件仍为9月7日，本次未逐张视觉复核；不得将前端Mock当部署。

新增每需求api/cases.json、API说明和日期执行记录。scripts/run-requirement-api.py为CLI；filbet/requirement_api.py复用smoke认证/CBOR、qa_core环境与本机锁，严格验证Case引用和断言；只执行白名单查询，空样本不充当筛选通过，金额使用Decimal，结果按需求/时间隔离且不落原始响应或个人资料。配置/复核/派奖/导出闭环仍依赖专用样本或正式契约，未执行写业务请求。没有自动将整条混合验收Case标PASS。

真实调用及定向复测证据见各需求api/execution-2026-09-10.md；本地初测20260910T064048360549Z、复测20260910T064317776852Z。首次沙箱联网失败独立保留，解除网络限制后完成真实调用。FAT鉴权精确错误标记status=false/data="token"已核实，首次脚本识别失败保留，修正后8条通过。Multi主单与明细合计差3600000.01；空列表null及缺时间仍成功属契约差异；返利0.40对conf0.405属于Q-05精度待确认，不直接断定发奖缺陷。明细见各单记录，部署版本未提供。

执行期间发现scripts/qa_core/process.py与windows_job.py出现其他未提交修改，本任务未编辑这两个文件，继续保留；结束时另发现scripts/qa_delivery/未跟踪目录，同样不是本任务创建，保留。未修改真实环境文件、P0用例和入口，未commit/push或向Jira发送消息。后续需先确认上述契约/样本/部署，再补充KYC隔离双角色、返利结算与独立对账、导出文件链路；不要只重复查询制造整单通过。

本轮最终本地验证：`npm run check` 通过（97份文档、235份归档、185份源码语法、190条单元测试，其中新增7条需求API回归）；git diff --check通过。单元测试是离线断言，不代表上述尚未执行的业务闭环通过。

### 2026-09-10 Telegram 提测与测试群评审接入

用户确认并授权实现：提测群触发，API执行后扫描/生成/执行UI，报告与候选BUG仅发测试群并@对应测试；负责人确认具体BUG后批量创建并关联Story。沿用原需求与P0资产，当前需求API执行器来自并行的用户工作，保留原改动与实际执行证据，本接入任务未重跑业务API。新需求UI授权取代此前默认只人工的全局约定，未扩P0清单。

新增qa_delivery队列/outbox/确认快照、API适配、声明式Playwright扫描与执行、Codex结构化计划/缺陷分类、Jira逐项去重与不确定提交恢复；CLI为`npm run qa:telegram`。公共进程模块仅增加可选timeout，保持默认命令兼容。完整配置/命令/边界见 [Telegram手册](../../docs/telegram-qa.md)。

用户指定在本机运行；机器人、提测/测试群及Davinci、ukrwlof、Rayleigh的身份线索仅写入忽略的config/telegram/local.json，未将群ID/邮箱写进可提交模板。Telegram数字用户ID仍待绑定，现有仓库.env未找到Telegram/Jira凭据。已生成忽略目录下的两个launchd配置，尚未加载或启动监听。版本查询入口、真实UI登录/页面/请求白名单也待首个Story接入；缺少配置时任务阻塞，不猜部署版本或沿用旧结果。

通过现有Jira连接器只读核对ISOP项目元数据：Bug=10676、Story=10677均为层级0，Relates链接类型存在；配置采用创建真正Bug并关联Story，不伪装子任务。未创建Jira问题或发送群消息。常驻进程需要本机REST凭据，不复用此对话连接器身份。

本地验证进行中：已验证持久去重、两群分流、负责人/版本/具体BUG确认、Jira断线不重复创建；本机回环HTTPS真实浏览器夹具验证扫描、查询成功与未授权请求BLOCKED通过。初始夹具空ready节点不可见已修正；该夹具不连接业务系统。最终全库检查结果见后续记录。

### 2026-09-10 改为手动单次扫描

用户明确不要长期监听，改为手动命令触发。`npm run qa:telegram` 默认scan：非等待分页接收未处理机器人消息，执行已入队测试，报告只发测试群，处理此前已确认BUG后退出。保留用户原定的负责人确认环节；报告发出后的新确认在下次执行同一命令时处理。移除poll/worker/service-files模式及此前生成但未加载的两个plist，未启动或停用任何系统服务。

默认读取用户已配置的requirements/.env.telegram，仅本地检查TELEGRAM_BOT_TOKEN存在，不输出值；Jira凭据/提测人数字ID/部署版本查询/UI实际页面规则仍缺失，不能宣称业务链已接通。Bot API待处理消息最长保留24小时，不提供任意群历史扫描；普通文字不作为可执行提测解析，超过期限需要重发有效指令。

新增4条离线回归验证一次扫描执行/退出、下一次读取确认后才建BUG、有限分页与游标、空批次及重复更新不重跑。此前全库检查受沙箱进程组检查和回环监听限制未全通过；本轮最终校验见后续记录。保留全部已有需求/API修改，未发送群消息、创建BUG或执行业务测试。

补充：新增identify单次身份读取，空submitter_ids时scan在取消息前停止，避免未配置发起人时消费提测。全库校验发现macOS在SIGTERM后、进程尚未回收的短窗口对进程组返回EPERM；本机最小复现确认随后变为ESRCH，公共清理现只在原3秒期限内重试此瞬态，持续拒绝仍由最终SIGKILL报错，不放弃后代清理。

本轮最终验证：`npm run check` 全部通过（98份文档、235份归档、192份源码语法、214条单元测试，含本机回环HTTPS真实浏览器夹具）；`git diff --check` 通过。新增单次批处理5条用例再次定向验证通过。未触发真实Telegram扫描/发消息、业务API/UI或Jira写入；未提交、未推送，尚待补齐本地接入配置后执行首轮真实批次。

### 2026-09-10 README常用命令与接入清单

按用户要求将`npm run qa:telegram`放入根README的日常入口附近，说明单次执行、测试群报告与确认后的再次提交。Telegram手册新增首次接入清单，区分人员数字ID/提测权限/Story分派、Jira本机凭据、首个试跑页面与部署版本来源；已有群路由、Token和本地业务账号不要求重复提供。只改文档并检查本地配置是否存在，未读取群消息、运行测试业务或创建BUG；本轮文档验证见后续记录。

本轮验证：`npm run check:docs` 通过（98份），`git diff --check` 通过；`qa:telegram -- check`确认Token有值，Jira凭据缺失、提测人ID列表为空、FAT/UAT版本查询未配置。未校验凭据有效性或连接外部系统。

### 2026-09-10 Telegram改为先预览确认，凭据只读验收

以下取代前文自动扫描即执行、提测人白名单与强制版本的旧说明。所有提测群成员均可提交“工单+提测/提測”；本机`npm run qa:telegram`只扫描并保存本地预览，通过Jira真实parent核对子任务所属Story。用户确认候选ID和预览revision后才用run执行；当前pilot_story为ISOP-2027，默认FAT。未核实Story、环境冲突或过期预览不执行；未提供部署版本记录限制，不单独阻塞。测试负责人全部待认领，仅三位测试需要数字ID；测试群确认具体BUG后用submit提交并关联Story。README、命令与Telegram手册已同步。

用户已在根目录.env.telegram配置Token/Jira凭据，CLI默认路径相应改为根目录，requirements下旧文件不再默认读取。真实只读verify通过机器人身份/Webhook、两群可达性、Jira自身身份与配置邮箱匹配、ISOP-2027 Story核对。Python默认CA缺失，已在忽略的本机配置指定QA_CA_BUNDLE=/etc/ssl/cert.pem，HTTPS保持验证。未读取getUpdates、发消息、创建BUG或执行业务API/UI。

剩余接入：机器人当前隐私模式且非群管理员，普通@测试的提测消息不可见；需BotFather关闭privacy并重新加入提测群，CLI在此配置完成前停止扫描。三位测试数字ID仍未绑定，可在测试群whoami后用identify读取。FAT现有ADMIN_EMAIL不匹配用户指定的Rayleigh业务账号，需要提供该账号成套本地登录配置；已加执行前账号匹配检查，未改原FAT账号/密码。UI实际页面/认证/请求规则尚未适配，记NOT_RUN；不能声称完整API/UI流水线已通过真实验收。

本轮 `npm run check` 通过：98份文档、235份归档、193份源码语法及217条离线单元测试（含回环HTTPS浏览器夹具）；git diff --check通过。保留全部用户原需求/API修改，未提交、未推送。下一步先完成隐私设置和身份配置，再由用户执行扫描、核对2027候选并确认执行范围。


2026-09-10 账号澄清：用户授权沿用现有FAT后台测试账号，此前邮箱仅指Jira身份。用户口述简称与ADMIN_EMAIL完整值不完全相同，未据此覆盖任何凭据；已按“使用已经配置好的账号”修正忽略的config/telegram/local.json中account_email为现有FAT有效ADMIN_EMAIL。未改后台账号、密码、登录验证或Jira凭据。此前“需更换FAT账号”的阻塞项撤销。离线配置检查、文档检查与差异空白检查通过，未登录业务系统或启动测试。


2026-09-10 测试人员身份绑定：根据用户提供的identify结果，已将Rayleigh、Davinci及ukrwlof（本次显示名ukrnbm，无用户名）的三个Telegram数字ID写入忽略的本地配置，保留既有Jira邮箱映射。数字ID不写入可提交文档；此前“三位ID未绑定”已解决。所有成员仍可提测，测试任务仍待群内认领，不自动分配。离线配置及文档检查通过；未读取群更新、发消息、执行业务测试或创建BUG。


2026-09-10 AI提测识别优化：用户确认不需要脱离AI，已替换生产入口的关键词capture。先持久保存脱敏提测群消息，再调用现有Codex CLI结构化分析；每次100条新消息加50条历史上下文，保留逐条判断/引用依据/范围，真实Jira核对父Story。失败可本地重试，AI编造工单、漏判消息会拒绝入队；未分析完禁止确认，歧义需补充，撤回可替代未确认候选。终端显示统计与分析状态、理由，空清单不再提示run。未读取用户新发群消息，未启动真实AI分析或业务测试，未发群消息/创建BUG。CLI参数已用本机help核对；首轮真实语义质量仍待用户执行命令验收。此前13条更新未存原文，无法追溯具体为何未匹配。

本轮最终 `npm run check` 通过：98份文档、235份归档、194份源码语法和220条离线测试；`git diff --check` 通过。AI识别测试采用模拟输出，涵盖失败持久重试、无关键词消息、伪造工单拒绝、撤回与确认隔离；没有把离线验证记为真实模型识别成功。

2026-09-10 真实消息JSON失败修复：只读核对SQLite，两条待分析消息正是用户截图中ISOP-2051“已經上fat了”的转发和随后“我在测试，不用理”。失败发生于AI调用前：对序列化JSON整体执行safe_text将整数时间戳替换成未加引号的[NUMBER]，触发JSONDecodeError。改为递归脱敏字符串值，保留数字/null/结构；同步AI上下文、报告与人工修订入口。使用已保存两条消息验证JSON往返成功，队列仍待分析，未调用真实AI、扫描新更新、执行测试或发消息。下一次命令可重试本地消息，不要求重发；应结合后句判断是否仅为联调消息，不预设成正式提测。

本轮 `npm run check` 通过（221条离线测试，新增时间戳/带引号凭据脱敏回归）；`git diff --check` 通过。旧preview的FAILED仍代表上轮结果，未伪造改为成功；真实AI语义识别待下一次扫描验证。

2026-09-10 AI进程启动失败修复：最新preview为FileNotFoundError，intake上下文与schema正常、日志为空。用户终端使用codex短命令未找到程序；本机已有VS Code扩展内Codex。已将忽略的local.json中codex_executable设为当前已安装绝对路径，并在共享AI入口增加可执行文件预检查、终端输出具体错误。使用仅/usr/bin:/bin的PATH验证绝对路径可运行--version（0.153.0）；扩展升级后如路径失效需要更新本地配置。

本轮尝试用已保存消息做真实AI验证，被自动审批拒绝：审批要求用户明确同意将这两条内部Telegram消息发给Codex模型服务。未绕过、未执行真实分析，待用户明确批准；两条消息仍保留，未扫描新消息、发群消息、启动测试或创建BUG。离线文档/归档/资产/语法检查通过；首次单测仅因沙箱禁止回环绑定失败，允许本机夹具后单测重跑结果见本轮工具输出。diff空白检查通过。


2026-09-10 扫描时间与重复记录治理：用户要求时间节点/选择、当日重复整理及ISOP上下文过滤。新增scan_history模块；默认继续已有Telegram offset并记录扫描起止/上次时间、前后游标，--since接受带时区ISO时间筛选本机与可用消息（旧数据保留排除状态，可选更早时间恢复；已分析不重跑）。每天仅一份scans日期JSON汇总次数/更新数/最新结果，不无限追加空扫描记录。AI输入以ISOP线索、回复和前后5条/30分钟邻近上下文为范围，不依据提测关键词决定意图；上下文范围有限，跨远距离无回复关联需明确补充。失败重试使用稳定证据摘要目录，保留待分析消息；未完成分析仍禁止确认执行。

本机既有4个旧随机尝试目录合并为reports/telegram/intake/legacy-attempts.zip，按内容哈希去重为3份文件内容；manifest保留原目录/文件映射，逐文件回读确认一致后清除旧目录。未删除SQLite、待分析消息、preview、已确认任务或业务报告。IDE已打开的旧intake文件可从归档找回。未调用真实AI、读取新群消息、发消息或执行业务测试；此前自动审批对真实消息外发验证的阻塞仍未解除，本轮不绕过。

本轮 `npm run check` 通过：98份文档、235份归档、195份语法文件、223条离线测试，新增时间窗口恢复/每日汇总/ISOP筛选/失败目录复用回归。最终文档和差异空白校验通过；真实AI识别仍未验收，旧preview保留上轮真实失败记录。


2026-09-10 用户明确AI只整理提测工单、测哪些由用户决定。移除WITHDRAWN自动SUPERSEDED与UNCERTAIN禁止确认的逻辑；有工单的AI判断均保留候选，歧义/撤回仅备注，Story/环境核实及人工确认仍保留。提示词明确“我在测试、不用理”不能覆盖部署通知。消息8380/8381已恢复待分析，preview明确标PENDING_REANALYSIS而非伪造AI成功；保留既有原始分析文件/消息游标，未调用AI或发送外部请求。用户此前终端输出表明实际AI曾完成分析但语义误判，不能沿用为新规则验证。


2026-09-10 按需求归并：只处理消息自身正文/caption/链接中的ISOP编号，排除普通聊天。新增design明确子任务行的本地映射，冲突不猜；找不到才查询Jira。preview按Story/环境/版本/范围归并并保留全部成员与来源工单，确认产生一次测试且标记所有成员。run在确认前逐一查Jira父级，一致才入队。新增明确映射/冲突排除回归并调整按需归并用例，全库check通过224条离线测试。详细下一步见docs/telegram-next-session.md；本轮未访问外部服务或执行真实测试。
