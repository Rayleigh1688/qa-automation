# 2026-09-10 仓库维护扫描

> 留存说明（2026-09-11）：本文是原日期的文字结论。用户已授权删除旧结果、扫描和临时脚本，文内旧路径只表示历史来源，不能作为当前可复核/可导入证据；当前入口与保留范围见[清理记录](project-cleanup-2026-09-11.md)。

[当前交接](../AI-HANDOFF.md) · [架构](architecture.md) · [命令](commands.md)

## 扫描范围与方法

扫描当前受Git管理及非忽略新增文件，覆盖scripts、ui、tests/unit、config、npm入口、Jenkins/GitHub工作流、docs与requirements。当前有效源码盘点144份；使用Python AST检查重复函数体、文件规模与引用，再针对命中及近期变化人工核对依赖、调用与测试。文档核对当前规则、命令、导航和历史来源。不是逐行证明全仓库没有缺陷，也没有执行业务环境扫描。

开始时工作区已有需求API、Telegram以及公共进程模块等未提交修改。全部保留；不将这些既有改动计为本次新增成果。忽略的环境、SQLite、实际报告及KYC素材没有迁移、清理或重建。归档仅校验完整性，不改历史扫描脚本；未commit/push、发群消息、创建BUG或执行联网API/UI。

## 已落实

| 问题 | 处理 | 保持的兼容边界 |
| --- | --- | --- |
| 多个脚本复制相同Markdown表格逻辑 | 四个脚本复用qa_core.reporting.markdown_table | 原table名字及非空表格输出保持；支持空输入；带额外换行清理的主流程表格保留 |
| smoke与negative重复嵌套字段判断 | 提取qa_core.values，smoke保留导入导出 | present-null与missing语义分开，不改变接口断言 |
| API/UI/full runner重复错误脱敏与命令显示 | 提取qa_core.redaction | 每个入口原敏感参数集合保留；执行编排及退出行为不合并 |
| Telegram复制JSON/CBOR解码 | 复用UI业务响应模块 | 不改变业务成功判定或保存原始资料 |
| 通用report_verdict遗漏BLOCKED明细 | BLOCKED结果不能返回PASS | 补充仅BLOCKED及PASS/BLOCKED混合回归；不重写任何历史报告 |
| Telegram入口混合应用处理，Store残留旧/test解析 | 入口收敛为兼容启动脚本，命令处理移入qa_delivery.cli；移除仅测试调用的旧解析器 | npm/CLI参数、main导入名、ROOT和产物路径保持；测试夹具不再把废弃协议留在生产代码中 |
| AI-HANDOFF累积463行历史及互相覆盖的旧约定 | 当前交接收敛为35行，完整旧文本冻结到历史文档 | 保存原文SHA256；相对链接迁移逆转换与原文完全一致，日期/来源/已确认决定不丢失 |
| 部分需求流程/契约模板仍写群内认领和报告推送、UI一律人工 | 同步唯一Story负责人、本地评审、全部建单后单次群清单及专项UI入口 | 历史证据原样保留；未适配UI不虚称已完成；P0门禁不扩范围 |
| 导航检查将本机需求/Telegram报告当成必备文件 | 仅明确生成物目录允许缺失 | cases.json等源码/资产及其他路径仍严格检查，干净checkout不依赖本地证据 |
| 新公共模块未进入复用说明 | 更新架构及导出白名单 | 显式导出仍不带业务、凭据、配置或报告 |

## 不应直接删除或合并的内容

- 旧CLI、兼容导出和npm别名仍被现有命令/测试使用，是兼容层，不是可直接删除的死代码。
- 大文件包括主流程报表、API/P0编排、业务page对象；文件大不等于重复。已检查引用及职责，暂不因行数搬移关键资金逻辑。后续按明确变更再拆分，避免制造跨平台和恢复路径回归。
- API与UI结果、验收Case与执行组合各有不同职责。当前2027报告的BLOCKED重复计数需要显式执行范围和验收聚合设计；本次不批量改历史状态，更不能把缺样本的用例改PASS。
- 原P0业务门禁、渠道、账号lane、归档目录和报告路径保持。长期运行产物仅是证据，不提炼成新的硬编码业务规则。

## 仍需单独处理

- Jenkins仍有全局固定登录码、环境注入和归档覆盖问题；本轮静态核对与[CI状态](ci.md)一致。未获得对应环境/部署验收证据，不顺手启用或重写CI业务流水线。GitHub代码通知与Telegram提测流程独立，未更改。
- ISOP-2027放开只读后的编辑/双人复核专项、第二有权管理员及状态样本准备，按[Telegram续接](telegram-next-session.md)继续；本次维护不等于执行授权或产品验收。
- 跨Windows/Linux实机、真实Jira建单及最后单次群清单仍需对应验收，离线模拟不能代替。

## 验证

最终`npm run check`通过：100份文档、235份归档文件、API资产、198份源码语法、233条离线测试。新增回归覆盖报告BLOCKED防误判、字段null/缺失、表格转义、脱敏及可选生成物链接；既有队列/审批/浏览器策略及独立运行核心导出测试继续通过。`npm run qa:telegram -- --help`验证兼容入口及原参数仍可用；`git diff --check`通过。没有运行联网业务门禁，也没有提交或推送工作区。
