# 项目清理记录

2026-09-11，按用户“删除历史版本结果、冗余代码并同步文档”的明确指令执行。此指令替代早期全部保留历史文件的约定；不再把旧产物搬到另一份归档。现有源码修改、需求规则、稳定用例编号和P0/npm/CLI入口保持。

## 已删除

| 类别 | 文件数 | 内容 |
| --- | --- | --- |
| 旧接口扫描 | 250 | 2026-09-07原始扫描、临时脚本、快照、截图与manifest |
| 旧需求报告与归并 | 343 | history、stage、team演示及旧运行目录；保留当前团队包和最近实测 |
| 旧专项API结果 | 126 | requirements各Story的api/results，含35个临时Python文件 |
| 旧UI试验与预检 | 61 | Telegram旧专项UI脚本、截图、预检输出 |
| 已完成P0运行产物 | 37 | 原UI报告、截图、会话输出及Playwright附件；生成目录占位保留 |
| 无消费者资产 | 1 | ui/data/isop2027-fat.json，当前使用isop2027.json |
| 当前包重复视图 | 8 | cases/failures/pending CSV与summary.json，完整结果仍在results.csv/html |
| 缓存 | 144 | 第一轮Python缓存与系统临时文件 |

第一轮共删除970个文件，约56.1 MiB，其中106个旧脚本。后续离线检查会临时生成缓存，收尾再清除；这些缓存不计业务结果。

## 保留及验证的边界

- 当前[团队执行包](../reports/qa/ISOP-2027/team-ready-20260911/README.md)、[API表](../reports/qa/ISOP-2027/team-ready-20260911/api-auto.csv)、[人工回填表](../reports/qa/ISOP-2027/team-ready-20260911/manual.csv)保留原批次及内容；准备视图不等于已执行。
- [最近真实执行](../reports/qa/ISOP-2027/latest.html)保留原始结果、计划/用例/页面资产快照、检查点及Restore失败截图。latest仍指向20260911T050501Z-d626392b，不是完整API或人工回归结果。
- Telegram任务58bed509adb9b8a9仍待评审；该任务目录、SQLite和消息/扫描/去重状态不改。环境文件、号码预留及既有api/local-state不改。清理前后核对49个保护文件hash一致。
- 旧测试会员/申请标识收敛到本机忽略且0600的api/local-state/requirement-records-fat.json，仅供查找异常现场；不保留完整个人资料快照，不是实时状态或执行证据。本次未处理业务现场。
- 日期文档保留必要业务结论，并注明旧原始来源已清理；候选BUG不是已确认缺陷。旧API来源不能再导入，提单前需补当前版本证据。

## 防止重复产物继续增长

新qa:requirement执行/重建及qa:delivery准备/导入默认只生成results.csv/results.html。HTML保留搜索、FAIL/ERROR/NOT_RUN筛选；显式--extra-views才生成额外四份视图。原始结果、快照及必要证据仍保留。

公共报告默认行为兼容原消费者，旧P0、查询CLI及qa:report输出不改。旧扫描校验实现已删除，check:archive入口改为验证退出目录只含索引。没有增加新的自动清理任务；后续清理需继续识别当前执行包、活动任务、恢复状态和证据引用。

当前入口、团队流程、阶段计划、架构、命令、需求API/BUG及Telegram续接已同步。历史文档只表示原日期结论，不能当成当前命令或证据来源。

## 本次清理验证（历史记录）

- `npm run check`通过：112份文档、旧扫描退出检查、API资产（1136项索引/911个请求/57条P0用例）、170份源码语法、269项离线测试。测试中的业务状态输出来自本地夹具，不是FAT调用。
- CLI验证：ISOP-2027默认71项、65项有执行步骤，未登录；在/tmp生成和导入执行包，默认两份结果视图、显式extra-views六份视图均符合约定。Telegram help仍兼容，未启动扫描/投递。
- 本机Chromium核对当前准备视图89条、最近UI批次18条，FAIL/NOT_RUN筛选、编号搜索、CSV下载及全部本地证据链接通过；禁止外部请求。
- 清理后再核对49个保护文件hash不变、私有索引0600、latest指针未变，ISOP-2027仅剩当前两个目录。文档同步后`npm run check:docs`和`git diff --check`通过。

本次没有执行FAT/UAT业务、数据库写入、BUG提交、群消息或Git提交；人工尚未执行，Windows/Linux/CI实机仍未验证。旧API原始证据删除后无法离线复核或导入，需新批次补证据；日期文字结论不等于当前通过。

## 提交前整理（2026-09-11）

此次提交汇总本会话累积修改；上面的清理数量与检查结果是当时记录，不替代本节提交前核对。保留用户已有源码修改及已授权删除，未暂存、提交或推送。

| 改动组 | 提交内容 |
| --- | --- |
| 新需求执行与结果 | qa_core公共计划、执行、报告和交付模块；FILBET需求API实现；ISOP-2027统一计划；保留旧查询和P0入口 |
| 用例与团队分工 | 13个需求的业务总表、已有4个需求的API数据表、模板与导出器；API自动执行、UI人工回填；2030当前仅手工UI |
| Telegram流程 | 消息持久化、需求级预览、明确任务确认、API/人工结果合并、版本声明/核验、本地BUG评审和确认后的批量提交；示例配置可提交，本机配置不提交 |
| 维护与清理 | 删除236个已跟踪旧扫描文件；兼容check:archive；公共进程/报告/字段读取复用；命令、架构、现行交接同步 |
| 本轮提交修正 | 文档校验允许缺省的reports/qa本机产物，仍检查需求/源码必需文件；长期文档移除实际测试手机号，单元测试改用独立模拟号码 |

提交候选包括新增文件，不能只查看git diff；其中plan.json、API数据表及历史交接是已有设计/兼容依据，未为缩小提交而删除。大量删除均属于已批准退出的旧扫描归档，不是P0用例或接口目录被删除。

`.env*`（除模板）、`config/telegram/local.json`、`reports/`、`api/local-state/`、需求运行结果、截图及数据库文件继续忽略。机器上的Codex绝对路径、负责人和2030 test_scopes配置保留本机；新机器按[Telegram手册](telegram-qa.md)配置，不能从Git获得当前队列或本地证据。没有把忽略文件加入暂存区。

建议提交标题：`feat(qa): 统一需求测试交付与提测确认，清理历史扫描资产`。

提交前状态为58个修改、113个新增、236个删除，暂存区为空。最终`npm run check`通过：112份文档、旧扫描退出校验、API资产、177份源码语法及294项单元测试；`git diff --check`通过。另将全部365个候选文件复制到不含本机报告/私有配置的临时检出目录，112份文档检查通过。已知本机敏感值比对及常见凭据模式检查未发现待提交泄漏；环境文件、队列和报告未被Git跟踪。103个当前报告、业务定位状态与本地配置文件hash保持。

本轮只做本地整理、模拟测试和只读核对，没有再次扫描群消息、调用AI分析、执行FAT/UAT业务测试、提交BUG或发群。此检查不代表新需求业务验收通过，也不代表Windows/Linux/CI实机验收通过。

新对话以[当前交接](../AI-HANDOFF.md)为入口。先本地preview核对待确认需求；2022/2085的环境、契约和独立样本仍待核实，完整测试条件未齐。2030只做手工UI，2027的UAT请求不当成已部署；BUG提交继续等待具体清单确认。

## 远端合并（2026-09-11）

用户随后明确授权“不要覆盖，合理合并然后推送”，替代上一阶段由用户自行提交的安排。合并前保存完整源码快照及本机状态hash；本地全部累积改动独立提交为ed0b5da，再三方合并origin/main的99c1d9c及其之前4个新增提交，不使用整仓ours/theirs或强制推送。

三个文本冲突位于AI-HANDOFF、2022/test-cases和2027/test-cases，按以下语义处理：

- 保留本地精简交接、执行框架、用例分离、已批准清理和全部执行记录；加入远端Windows doctor检查，不将连通性PASS解释为2022业务测试通过。原远端范围到R30，本地新增R31保留。
- 保留远端2027表内人工填写的“通过”、Davinci负责人以及完整C18；合并AC-03的新分行游戏类型限制规则和Q-02关联，同时保留本地新增问题与历史异常。人工状态标明来源提交，原环境/版本/证据待关联，不导入自动结果或覆盖本机manual.csv。
- 2027业务总表同步为18条，原71个API组合和冻结plan保持；C18需补限制配置、同步接口、独立对账及自动断言，在契约评审与PRE-10标明。旧17项覆盖表继续作为原日期快照，不追加虚构的C18执行结果。
- 新增.gitattributes限定需求CSV的换行约定：Git存储规范化文本，工作区保留CRLF和UTF-8 BOM。避免新增CSV的回车被误报为行尾空格，也避免跨平台合并整文件换行差异；不改报告内容或CSV字段。

当前运行报告、业务定位状态、凭据和本机配置不随合并迁移；没有发起业务API/UI、AI扫描、BUG提交或群消息。此合并只同步源码和已存在的不同来源记录，不把任何一方的局部或人工结论扩展成全量自动化通过。

合并验证：`npm run check`通过，294项单元测试包括新增业务用例不冒充旧API覆盖的检查；文档、语法、API资产和旧扫描退出校验均通过。冲突标记及索引未解决项为零，暂存差异检查通过。逐行核对远端18条人工记录完整保留；本地plan.json、api/cases.json和71条API数据表字节不变，109个本机环境/状态/证据文件hash不变。提交与远端状态以Git日志/分支为准。
