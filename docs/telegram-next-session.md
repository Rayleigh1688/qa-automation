# Telegram 自动测试续接

更新：2026-09-11。当前新需求采用[API自动执行、UI人工回填](team-testing.md)，暂不建设新需求UI自动化；P0的API与核心UI自动化继续维护。Telegram新任务已接入统一API执行及人工包回填；旧任务保持原报告，不自动迁移。本次全项目清理没有启动机器人、投递消息或修改队列，范围见[整理记录](project-cleanup-2026-09-11.md)。

## 最新扫描与CLI修复（2026-09-11）

此前代理发起的扫描被自动审批拒绝，未执行。随后用户自行扫描，贴出的08:00:59Z—08:01:01Z终端日志显示收到11条更新、保留1个候选、4条消息待AI分析；AI因配置引用已退出的VS Code扩展26.901.22334而未启动。此状态不代表没有新提测，也不能据已有ISOP-2030候选认定分析已完成。

已只修改忽略的`config/telegram/local.json`中的`codex_executable`，指向本机`command -v codex`解析到的26.908.31457扩展内`bin/macos-aarch64/codex`。本地`--version`返回`codex-cli 0.154.0-alpha.6.1`，`exec --help`支持扫描器使用的参数，离线配置检查通过。此次修复没有改写队列、游标、任务hash或历史证据；不把配置变更套入旧任务确认。

用户随后提供的08:14:13Z—08:14:37Z扫描日志已显示AI分析完成，本地保存的preview与之相符：ISOP-2085归到2022；2077与2032合并到2032；2030为已有候选；2027只有请求更新UAT的消息，明确标为待核实。2028没有待确认提测记录。2022/2030/2032的FAT来自缺省配置，消息未明确环境，仍须核实；不能把这份列表当作完整测试条件已齐。

2030范围纠正：本条只有H5提测，需求设计为页面改版，当前没有独立API变更依据。旧scopes的api/ui来自扫描器默认值；已将本地stories.ISOP-2030.test_scopes限定为ui，8条总用例统一手工UI。预览/确认/执行取限定范围交集，原候选与历史证据保持；配置变化后使用刷新生成的新revision。没有自动执行UI、补建API资产或调用业务接口。

## 需求级待确认列表（2026-09-11）

用户确认以requirements现有需求目录为列表单位。终端及preview.md每个需求一行，原始消息、AI判断、负责人、部署版本和全部用例缺口保存在preview.json；目录外/未核实所属需求的条目留在unmatched，不删除消息。同需求不同环境、版本或范围保留独立批次，按需求编号选择遇到多批次时必须改用候选ID明确选择。

`npm run qa:telegram -- preview`已离线刷新本机列表，只读SQLite，不扫描、不调用AI或执行测试。当前预览版本以生成文件为准，不沿用用户旧日志中的revision。确认方式见[Telegram手册](telegram-qa.md#手动扫描一次)。

验证：2030范围修正后`npm run check`通过，294项单元测试包含本轮累计9项需求选择/显示/范围检查；覆盖目录外工单不可确认、子任务合并、多环境/版本/范围歧义拒绝、旧revision失效、AI失败不提示执行、只读刷新不改队列，以及UI限定不启动API、范围无交集拒绝确认。既有3项本地HTTP/浏览器夹具在允许127.0.0.1端口后通过，不连接业务环境。实机只执行本地preview、配置检查和2030用例导出；47个队列/扫描与任务证据文件校验值保持，本地配置仅新增2030的test_scopes限定。未重新扫描、调用AI、运行API/UI业务或提交BUG/发群。

## 当前任务

任务`58bed509adb9b8a9`仍为REVIEW，来源为ISOP-2051、消息8380，父Story核对为ISOP-2027。负责人Davinci。当前本地报告revision为`9c2967a3d3e1d368`；旧群中revision无效。该任务目录`reports/telegram/runs/58bed509adb9b8a9/`和SQLite、扫描/消息游标、去重状态均保留，不按普通历史报告删除。

提交前只读核对：该旧任务未获BUG批准，其config_hash与当前本机配置已不同，不能直接沿用旧报告确认提交，也不改数据库hash绕过校验。保留原报告作为历史参考，后续产品BUG应在当前配置的新批次补复现并重新评审。

这是2026-09-10的只读批次，原范围为6个API组合及C12部分UI列表/筛选交互。API空待复核列表返回null与数组断言不符，仍为CONTRACT；UI Search定位问题修正后只定向复测两项。原报告、initial-report和ui-retest-1均保留，不能表述为同一轮无中断全通过或整个Story通过。当前任务没有获批准的PRODUCT BUG，Jira建单记录为空。

原流程曾发过程通知，用户随后明确否决；当前规则禁止测试过程发群。不得重启后补发旧报告/通知。当前规则和恢复命令见[Telegram手册](telegram-qa.md)。

## 已生效的用户决定

- 本机手动`npm run qa:telegram`，不常驻监听；只将消息本身含ISOP编号的内容交AI整理。优先使用design的明确子任务映射，执行前逐个来源工单查Jira核对父级。
- 按Story、环境、版本及范围归并；每个Story固定一名负责人，子任务和后续批次沿用。ISOP-2027由Davinci负责；Jira身份与FAT测试身份分开。
- ISOP-2027/FAT后续API读写、边界、权限测试沿用既有授权，不重复询问；按最新分工，需求UI交人工执行，不自动续跑旧UI专项。ISOP-2030新提测不在本轮；未提供部署版本只记录限制。
- 具体BUG提交仍需确认。测试过程、报告和候选只留本机；获确认的PRODUCT项整批建单并关联Story成功后，才一次性发群清单，过长用单个文本附件。部分失败或无PRODUCT项不发。
- Jira BUG状态变更推送已由用户确认全面通过，不检查或修改该脚本。数据库只读；B保持用户指定Codex角色，不临时升权冒充普通角色通过。

## 当前证据和后续工作

- 当前团队执行包为`reports/qa/ISOP-2027/team-ready-20260911/`，人工尚未回填；最近真实需求执行在`reports/qa/ISOP-2027/latest.html`。使用规则见[团队流程](team-testing.md)。
- 后续API/UI专项的旧结果、阶段汇总及结果目录临时脚本已删除，不再从旧路径继续或导入。业务规则与日期结论保留；候选统一从[BUG清单](../requirements/ISOP-2027/api/BUG-review-2026-09-10.md)确认。旧API候选提交前需重新取当前版本证据，不能以旧Telegram空PRODUCT清单推断后续没有候选。
- 异常会员/申请的最小定位索引保存在忽略且0600的`api/local-state/requirement-records-fat.json`，属于历史状态，不是实时查询。不要复用旧会员或核准分行异常草稿来补成功；本次没有清理业务数据。
- 账号A/B和reviewer本地配置保留；普通角色新增复核权限节点、完整UI权限矩阵、状态1/4、精确原因最大长度、第三复核身份及故障恢复缺口见[准备清单](../requirements/ISOP-2027/preparation.csv)。已实现范围见[API阶段](new-requirement-stage1-2.md)、[可选UI阶段](new-requirement-stage3.md)。
- 下一步先让团队使用人工清单并回填，按本批范围执行API，再导入明确来源。真正Jira建单、Story关联、整批群清单和超长附件的线上验收仍未完成，离线检查不能替代。

凭据在忽略的`.env.telegram`，人员/负责人/UI配置在`config/telegram/local.json`，reviewer在`.env.fat.reviewer.local`。不将数字身份、token、证件或个人资料写进可提交文档。Codex可执行文件沿用本机配置，扩展升级导致失效时再重新定位。
