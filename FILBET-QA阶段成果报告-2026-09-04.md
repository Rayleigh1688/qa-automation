# FILBET QA 自动化项目阶段成果报告

**报告日期：** 2026-09-04  
**统计周期：** 2026-08-25 至 2026-09-04  
**覆盖范围：** P0 API/UI 自动化、FAT/UAT 主流程验证、FAT 接口发现  
**本期不包含：** Jenkins 建设、P1/P2 扩展、生产环境写操作、数据库业务数据修改

## 一、执行摘要

项目从 2026-08-25 的空白 QA 工作区起步，在约两周内完成了从测试规划、接口资产整理、API/UI 执行框架，到真实业务链路验证和多后台接口发现的第一阶段建设。

截至 2026-09-04，已经形成一套可执行、可复验、可交接的 P0 自动化基线：以 8 条端到端业务主流程为骨架，沉淀 57 条整体 P0 用例；FAT 完整验证已经通过，UAT 完成真实充值、投注、流水归零、提现和前后台订单核对。同时，客户端、主管理后台、合规管理后台、代理管理后台和代理前台五个调用端均已建立独立接口发现证据。

当前项目已经从“工具和用例搭建期”进入“稳定回归、接口补扫和资产治理期”。现阶段成果能够支持核心版本回归和问题定位，但接口发现数字仍代表已观察下限，不代表所有页面深层操作已经穷尽。

## 二、关键成果总览

| 成果项 | 当前结果 |
| --- | --- |
| 项目建设周期 | 2026-08-25 至 2026-09-04，共 11 个自然日 |
| 代码里程碑 | 15 个 Git 提交；2026-08-31 建立 P0 基线 `90b24e3` |
| P0 业务模型 | 8 条端到端主流程 |
| P0 用例资产 | 57 条：31 条 safe smoke、15 条 API 登记反例、10 条受控写/UI 协作项、1 条状态 UI 反例 |
| FAT 快速门禁 | safe API 31/31、默认反例 13/13、默认 UI 11/11 |
| FAT 完整业务链 | KYC、充值、真实投注、流水归零、提现及统一核对均通过 |
| UAT 主流程 | 8/8 PASS |
| UAT 统一资金链核对 | 10/10 PASS |
| UAT 默认 UI | 10/11；保留 1 个已确认产品缺陷 |
| 接口发现调用端 | 客户端、主管理后台、合规管理后台、代理管理后台、代理前台，共 5 个 |
| 动态观察首方接口 | 228 个唯一 `method + normalized_path` |
| 动态观察第三方接口 | 客户端 7 个 |

## 三、项目建设成果

### 1. 建立分层 QA 自动化工程

项目已经形成清晰的职责分层：

- `testing-plan/`：维护总体路线、阶段目标、门禁和增强计划。
- `skills/`：沉淀 API、UI 和业务规则等长期方法。
- `harness/`：维护调试经验、已知错误、环境限制和数据库只读诊断方法。
- `api/`：维护接口清单、分类目录、P0 固定资产、执行器及运行手册。
- `ui/`：维护 Playwright 用例、页面封装、控件数据、登录态和 Network 记录能力。
- `tools/provisioning/`：提供测试账号和前置数据准备工具，并与默认门禁隔离。
- `AI-HANDOFF.md`：建立新对话和新执行者的一分钟交接入口。

P0 资产已经明确唯一来源：`api/p0/test-cases.csv` 是完整用例索引，`main-flow-scenarios.csv` 只维护 8 条业务顺序，接口 shortlist 仅用于发现，不再直接决定 P0 范围。

### 2. 完成 P0 API 自动化基线

API 层已具备以下能力：

- 客户端和管理后台登录、鉴权及会话复用。
- JSON/CBOR 请求和响应处理。
- 动态时间窗口、环境变量和请求模板替换。
- safe smoke、负向契约、受控写操作分层执行。
- FAT/UAT 地址硬隔离，防止环境串用。
- 后台固定登录码与业务审核动态 TOTP 分离。
- 接口 HTTP 状态、业务状态、数据结构和必需字段的组合断言。
- 对 UID、充值单、投注记录、流水、钱包和提现单进行跨阶段关联。

当前 P0 固定资产包含 31 条只读 safe smoke，其中客户端 18 条、管理后台 13 条；另有 15 条登记反例，默认安全执行 13 条，充值限额相关 2 条作为显式缺陷探针保留。

### 3. 完成 Playwright UI 自动化骨架

UI 层已经建立：

- 客户端登录、主页面扫描、充值契约、提现契约和正反例用例。
- 浏览器 storage state 与 API token 的受控复用，减少重复登录导致的会话失效。
- DOM 定位资产、页面操作封装、环境加载和 Network 录制框架。
- 固定视口与坐标操作组合，用于第三方游戏或 Canvas 场景。
- 真实游戏身份及厂商校验，避免进入错误游戏后仍继续投注。
- UI 原始证据、可读报告和 API 结果分目录存放。

默认 UI P0 当前静态收集 11 条测试，并额外保留充值、提现、真实投注、未 KYC 提现拦截和页面控件清单等专项入口。

## 四、真实业务链路验证成果

### 1. FAT 验证结果

2026-09-02 FAT 全量验证结果：

- safe API：31/31 PASS。
- 默认安全反例：13/13 PASS。
- 默认 UI：11/11 PASS。
- 永久 BASIC 账号提现前置拦截：1/1 PASS。
- KYC 提交、后台审批、前台状态刷新完成闭环。
- 普通充值、后台补单、钱包变化完成同一订单关联。
- 真实投注、投注记录、账变和剩余流水完成交叉核对。
- 流水归零后完成合法提现，并与后台订单精确匹配。

FAT 受控写操作始终限定在指定测试 lane 或本轮创建记录；数据库只用于只读诊断，没有直接修改 KYC、余额、流水、充值或提现状态。

### 2. UAT 验证结果

2026-09-02 UAT 完成 8/8 条 P0 主流程和 10/10 条统一资金链核对：

1. 使用动态短信验证码完成登录。
2. Maya 普通充值 1200。
3. 进入 BNG `Coins`，以单注 100 完成 6/6 次真实投注。
4. 剩余流水从 535 降至 0。
5. 投注后可提现余额为 804。
6. 按合法档位提交 Maya 提现 500。
7. 后台定位到同一提现订单，状态为 `under_review`。
8. 投注后客户端 safe API 18/18、后台 safe API 13/13 通过。

UAT 没有数据库权限，因此流水和状态通过客户端及管理后台只读接口完成核对，证明主流程不依赖数据库写入或人工改数。

### 3. 前后台统一核对能力

项目已能围绕同一业务 flow 关联并检查：

- 用户 UID。
- 充值订单 ID、金额、状态和钱包增量。
- 投注次数、投注记录、结算/派彩观察和账变。
- 剩余流水及其归零结果。
- 提现订单 ID、金额、用户和后台待审状态。

这使测试结果从“接口返回成功”提升到“同一业务记录在多个系统中的数据一致”。

## 五、FAT 接口发现成果

接口发现统一以浏览器真实 DOM、路由、Network、动作前后状态为证据，并按 `method + normalized_path` 去重。当前动态观察结果如下：

| 调用端 | 动态首方接口 | 主要覆盖成果 |
| --- | ---: | --- |
| 客户端 | 59 | 页面初始化、导航、查询及主流程交互；另观察到 7 个第三方接口 |
| 主管理后台 | 135 | 57 个菜单页面、会员列表、17 个会员详情页签及受控写操作 |
| 合规管理后台 | 5 | 当前角色可见的单一 PAGCOR 报表路由、筛选、日期、查询和重置 |
| 代理管理后台 | 18 | 11/11 菜单路由、359 个 DOM 控件、71 个动作结论 |
| 代理前台 | 11 | 5/5 路由、47 个 DOM 控件、30 个动作或可见性结论 |
| **合计** | **228** | 五个调用端的独立动态证据 |

### 主管理后台深扫

主管理后台首先完成 57/57 个菜单页面初始化扫描，记录 844 条脱敏 Network 事件和 78 个唯一接口；在查询、重置、导航、筛选、弹窗和安全动作后扩展到 109 个动态接口；继续完成会员列表和详情内部补扫后，独立合并结果达到 135 个。

会员专项成果包括：

- 会员列表 30 个动作结论，覆盖 Batch、筛选族、组合查询、分页和行内入口。
- 会员详情 17/17 个业务页签完成 DOM 清单。
- 14 个数据页签形成 72 个 UI 动作结论和 76 条动作—接口映射。
- KYC 完成 `0 → 2 → 3 → 2 → 5` 的提交、拒绝、重提和最终通过链路。
- Bet/Login 限制、Risk Control、Wallet、VIP、充值倍率和 Turnover 完成可恢复操作并恢复原态。
- Wallet 为 `0 → 0.01 → 0`，VIP 为 `V0 → V1 → V0`，Turnover 为 `0 → 1 → 0`。
- 本期没有发现未恢复的意外副作用。

管理后台文档对照表当前有 581 条去重记录，其中 563 条来自接口文档，18 条为 UI 动态发现但文档未登记。该表是文档池与动态证据的并集，不代表 581 个接口均已通过 UI 验证。

### 多后台隔离扫描

合规管理后台、代理管理后台和代理前台分别使用独立 Playwright browser context 和独立登录态：

- 不共享 Token 或 storage state。
- 登录后校验实际域名、路由、当前用户接口和菜单权限。
- 账号、密码、OTP/TOTP、Token、Cookie、设备 ID 和原始个人数据不写入仓库。
- 未观察到的文档接口保持 `DOCUMENTED_UNVERIFIED`，不直接判为 `STALE`。
- 本轮三个新增调用端均未执行持久业务写操作。

## 六、质量、安全与可维护性成果

### 1. 证据可信度

- API、UI 和数据库证据严格区分，不用单一层面的成功冒充端到端闭环。
- 动态接口要求有真实 Network 事件；仅有 DOM 控件或接口文档时不标记为 `ACTIVE`。
- 接口动态分类仅使用仓库规定的九种状态。
- 导出以是否触发真实请求为完成标准，不因按钮被点击就虚报接口。
- 登录成功要求同时满足业务响应、目标域名、路由和已认证页面结构。

### 2. 数据与写操作安全

- 敏感凭据只存在于本地忽略配置或运行内存。
- 测试报告和扫描结果执行脱敏，不落盘原始账号、Token、Cookie、验证码和个人数据。
- 受控写只处理本轮创建或明确指定的测试记录。
- 任一业务阶段失败即停止，不调用后续成功接口制造通过。
- 数据库始终只读。

### 3. 环境稳定性

- FAT 与 UAT 配置、地址、账号 lane 和支付/游戏差异已集中管理。
- 客户端和后台会话支持复用及有效性检查，降低重复登录造成的 Token 冲突。
- UAT 动态短信 OTP 可通过同一短信 ID 在内存中获取，验证码不写入结果。
- 已记录旧接口、新接口和 GET/POST 方法漂移，防止只因 HTTP 200 而误判接口仍在使用。

## 七、已确认问题与当前缺口

| 类型 | 当前情况 | 影响 |
| --- | --- | --- |
| 产品缺陷 | UAT 未勾选登录条款仍可登录 | 默认 UI 为 10/11；缺陷未关闭 |
| 产品/环境问题 | FAT 充值通道上下限未被后端强制校验 | 保留显式缺陷探针，不进入默认门禁 |
| 环境限制 | 部分 UAT 支付通道不可用 | 当前正向链路使用已验证的 Maya |
| 接口发现缺口 | KYC 等页面仍有编辑、更新、分页及深层弹窗操作未动态触发 | 135 个主管理后台接口是已观察下限，不是完整总数 |
| 资产治理 | 多个接口发现目录位于仓库顶层，原始和最终证据较多 | 后续需要统一 schema、目录和结果生命周期 |

这些缺口不推翻已通过的 P0 核心链路，但必须在对外表达时区分“核心业务已通过”和“全量接口尚未穷尽”。

## 八、阶段判断

本阶段已经完成三个关键跨越：

1. 从无统一资产，发展到有明确 P0 范围、业务顺序、账号 lane 和执行入口。
2. 从接口级返回检查，发展到 FAT/UAT 真实充值、投注、流水、提现及前后台数据一致性验证。
3. 从单一客户端测试，发展到五个调用端的权限、菜单、DOM、路由和 Network 接口发现。

因此，当前项目可以判断为：**P0 核心自动化基线已经建立并通过实跑，具备持续回归和业务问题定位能力；下一阶段第一优先级是让 P0 完全由确定性代码执行，不依赖 AI 临场操作或判断。接口发现冻结为当前阶段快照。**

## 九、AI 协作方法沉淀

本阶段最重要的收获不仅是自动化资产数量，更是形成了更有效的人机协作方式：

1. LLM 对代码、接口文档、CSV、日志和规则文字等静态、可检索内容处理效率很高，适合批量分析、去重、映射和生成确定性代码。
2. 业务语义不清或缺少唯一正确标准时，AI 会把大量时间用于探索和猜测。明确的业务规则、前后状态、验收条件和事实源比扩大任务描述更重要。
3. 前端缺少稳定元素 ID、`data-testid`、语义化 role，或 DOM 结构频繁重绘时，UI 自动化容易卡住；需要通过稳定定位资产、Network 和状态证据共同补偿。
4. 一次交给 AI 过大的跨系统任务，容易因上下文、登录态、数据状态和业务分支过多而降低准确性。按页面、按流程阶段拆分后，任务更精准、证据更容易归因，返工成本也更低。
5. 高效模式是由人确定业务目标、正确标准和风险边界，由 AI 负责代码分析、自动执行、Network 录制、证据整理和报告生成，通过短反馈循环逐页推进。

## 十、下一阶段建议

1. 将 `npm run test:p0` 和 `npm run test:p0:full` 收敛为真正脱离 AI 的确定性入口：启动前自检环境、账号 lane、动态 OTP/TOTP 来源及依赖；执行中不等待人工输入；失败非零退出并自动生成脱敏报告。
2. 补齐 FAT/UAT 两套配置下的无人值守预检、阶段超时、失败归因和可重复运行策略，使操作者只需选择环境和 quick/full 模式。
3. 保持现有 8 条主流程和 57 条 P0 用例范围不变，先消除脚本之间的隐式人工检查点，再讨论新增覆盖。
4. 登录条款缺陷修复后重跑默认 UI 11 项，并移除临时接受例外。
5. 下一次完整资金链必须创建新的 flow，不重放本轮 UAT 充值、投注或提现订单。
6. 接口发现暂停；现有 228 个动态首方接口、各调用端证据和未验证文档池保持阶段快照，待用户明确恢复后再按页面继续。

## 十一、主要证据入口

- [当前项目交接与最新结果](AI-HANDOFF.md)
- [项目总说明](README.md)
- [P0 API 固定资产说明](api/p0/README.md)
- [接口发现计划与当前进度](interface-discovery/README.md)
- [主管理后台合并结果](fat-admin-interface-scan/results/member-gap-merged-report.md)
- [合规管理后台报告](pagcor-admin-interface-scan/results/pagcor-admin-report.md)
- [代理管理后台报告](agency-admin-interface-scan/results/agency-admin-report.md)
- [代理前台报告](agency-portal-interface-scan/results/agency-portal-report.md)
