# AI 对话交接

这是新 AI 或新对话进入仓库的第一份文档。它只保存当前状态、最新证据、已接受例外和下一步；长期方法、执行手册与排障记录由下层文档维护。

## 一分钟结论

- 当前阶段仍是 P0 收口，不扩展活动、VIP、代理、OCR/eKYC 等 P1/P2 矩阵。
- API 固定资产为 8 条主流程、60 条整体用例：31 条 safe smoke、15 条登记反例，其余为受控写、UI 协作或数据前置项。
- 2026-08-31 FAT safe smoke 31/31、默认安全反例 13/13 已通过。
- 默认 UI 全量串行复跑为 10/11：唯一失败是“未勾选登录条款仍可 OTP 登录”，网络已确认 `/member/otp/login/v2` 成功，属于产品缺陷；其余 10 条通过。
- 主账号采用单次密码登录，UI storage state 与 API session 双向同步 token；只读校验已证明复用时不会再次请求 client/admin 登录接口。
- 当前本地配置的只读、资金流和已 KYC 别名仍指向同一个已验证账号；账号状态清单已统一到 `api/p0/test-account-pool.csv`，下一步需准备独立资金账号、新 KYC 池、未 KYC 基础账号和低余额账号。本地配置已收敛为 `.env`，无效休眠候选及临时 overlay 已移除。
- 本轮非活动充值 1200 后生成基础流水 1800；真实 UI 投注按业务单注 1000 完成 2 注，数据库只读复查总剩余降为 0。
- 随后提现 1000 成功创建并能在后台按本次订单定位为 `under_review`。当前阶段按用户确认，以“订单成功提交并进入后台”为提现 P0 验收；真实转账成功在环境恢复后增强复验。
- 新账号 KYC 资料提交成功并进入 `Under Review`。最小 P0 仍缺后台审核和审核后前台状态刷新；证件/OCR/eKYC/驳回重提矩阵归 P1。
- P0 不需要继续堆接口数量。主要自动化缺口是本次业务记录的一对一关联断言，以及默认 UI 套件的充值/提现安全前置覆盖。

## 阅读链：由浅入深

1. 本文件：确认当前结论、例外和下一步。
2. [`README.md`](README.md)：全局目录、统一命令、CI、敏感信息和结果边界。
3. [`.agents/skills/filbet-p0-automation/SKILL.md`](.agents/skills/filbet-p0-automation/SKILL.md)：Codex 自动发现的任务路由。
4. [`skills/README.md`](skills/README.md)：选择 API、UI 或业务规则方法。
5. 子项目：[`api/p0/README.md`](api/p0/README.md)、[`ui/README.md`](ui/README.md)、`api/runbooks/`。
6. 发生失败时才进入 [`harness/README.md`](harness/README.md)。
7. 最后查看 `api/results/`、`ui/results/`、`ui/reports/` 的最近一次证据；生成物不是规则来源。

开始修改前执行 `git status --short`。工作区存在未提交修改，默认属于用户或当前任务，不得回退无关内容。

## P0 当前覆盖判断

| 流程 | 已有证据 | 仍需补充 | 当前判断 |
| --- | --- | --- | --- |
| 注册登录 | API 注册/登录、登录反例；UI 登录正反例已有实现 | 使用稳定账号全量复跑默认 UI 套件 | 基线已具备 |
| KYC | 新号真实提交、前台详情和数据库待审状态 | 后台审核本次记录；前台状态刷新 | P0 收尾项 |
| 充值 | 非活动参数确认、订单创建、后台补单、钱包和基础流水 | 固化本次 deposit id/uid/金额/账变的自动关联 | 功能已通，断言需加深 |
| 投注 | 本轮 Playwright 固定 1000，完成 2 注；流水 1800→0 | 按本次投注时间/标识唯一匹配 Bet History 和账变 | 动作已通，断言需加深 |
| 投注/派彩记录 | 列表接口 safe smoke 已通 | 轮询结算；金额、输赢、状态和流水增量关联 | P0 收尾项 |
| 钱包/账变 | 前后台列表和钱包接口已通 | 动作前后余额 = 本次账变净额；避免只证明“列表可查” | P0 收尾项 |
| 提现 | 本轮 1000 创建成功、后台 `under_review` 可定位 | 正向转账成功仅在 FAT 接口恢复后复验；客户端/后台子状态映射待确认 | 当前阶段接受通过 |
| 后台权限/报表 | 后台 safe smoke 13/13 | 报表按本次 uid/订单关联，汇总与明细一致 | 查询已通，断言需加深 |

## API 与 UI 资产现状

### API

- `api/p0/test-cases.csv` 是唯一完整用例索引。
- `api/p0/main-flow-scenarios.csv` 只有 8 行，只表示业务顺序和完成标准。
- `api/p0/interface-shortlist.csv` 是接口发现池，不能决定 P0 数量和执行顺序。
- 默认 safe/negative 已覆盖当前用户/权限、KYC 待审查询、充值/提现列表、财务报表、资金记录等结构契约。
- 下一步不是新增同类列表，而是在受控 runner 中保存并传递 uid、deposit id、bet id、transaction id、withdraw id，形成订单级关联断言。

### UI

- `npm run test:ui:p0` 当前静态收集 11 条默认测试，覆盖登录、主流程页面扫描、充值页安全契约、游戏启动和页面状态正反例；默认不真实充值、投注或提现。
- 默认套件固定 1 worker，并保留忽略的 storage state；API 固定保留忽略的 session 文件。同一账号的其他进程不得再次登录。
- UI 测试点共有 20 条：15 条 implemented、1 条 implemented_gated、1 条 covered、3 条 planned。planned 为充值缺金额反例及提现页正反例安全前置。
- `client-deposit-contract.spec.mjs` 已纳入默认 P0 命令；默认只验证页面、支付方式和金额控件，只有显式 `EXECUTE_DEPOSIT_CONTRACT=true` 才创建充值请求。
- 真实投注由项目 Playwright 自行启动 Chromium，不依赖 Codex 内置浏览器是否存在已连接实例。
- 最近一次完整默认 UI 套件已在复用会话下串行执行：10/11 通过，1 条为未勾选条款仍可登录的产品缺陷；真实投注专项另行 1/1 通过。

## 当前下一步（按价值排序）

1. 实现统一的关联上下文：每个受控阶段输出并复用本次 uid、订单号、钱包快照和时间窗口。
2. 完成 KYC 本次记录的后台审核与前台状态刷新。
3. 补投注/派彩、钱包/账变和后台报表的订单级/金额级关联断言。
4. 补充值缺金额反例及提现页“可打开 + 缺前置不发请求”的安全用例。真实充值/提现仍由 API controlled flow 完成。
5. 使用稳定账号统一复跑 API safe/negative 与默认 UI 套件，覆盖生成最新报告。
6. FAT 转账接口恢复后，新建一笔提现复验后台同意/成功；不得重试已经取消的旧订单。

## 不可违反的边界

- API、UI 和数据库分别提供契约、真实交互、诊断证据，不能互相冒充完整闭环。
- 数据库只读，不直接改 KYC、余额、流水、充值或提现状态。
- 受控写只处理当前 flow 创建的记录；任一业务步骤 `status=false` 时停止，不调用后续 success 接口制造通过。
- 密码、OTP、TOTP seed、token、cookie、设备 ID 和未脱敏个人资料只放忽略的本地配置或 CI 凭据。
- API 结果写 `api/results/`；UI 原始结果写 `ui/results/`；UI 可读报告写 `ui/reports/`。同名覆盖，历史交给 CI。
- 规则更新只改最窄的权威文档，再由上层链接进入；不要复制实时状态到 Skills、Harness 或多个 README。

## 对话结束检查

1. 同步当前证据和下一步到本文件。
2. 将长期规则、执行方法或已知错误分别写回 Skills、runbook 或 Harness。
3. 若用例范围/顺序变化，同步 `api/p0/` 固定资产；若只改变环境接受标准，不改目标场景定义。
4. 说明执行了什么、未执行什么，以及阻塞属于产品、环境、数据还是测试代码。
5. 检查没有提交密钥或带敏感信息的生成报告。
